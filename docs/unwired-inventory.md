# 未反映設定 (unwired) の棚卸し

GUI の設定フォームには「計算へ反映されない」ものが残っている。本書はその
全件表で、`src/tabs/TabHelpers.h` の 3 種類の明示 (絶対規則 5) のうち
`unwiredNote()` が付いた **82 箇所** を対象にする。

## 3 種類の明示

| 表示 | 意味 | 件数 |
|---|---|---|
| `unwiredNote()` | 設定フォームがどこにも読まれていない | 82 |
| `markNotImplemented()` | ボタンを無効化 (押せない) | 81 |
| `sampleNote()` | 表示中の数値が実行結果でないことの明示 | 8 |

## 区分

| 区分 | 意味 | 件数 |
|---|---|---|
| **A** | 表示が古いだけで、実際には配線済み | 0 (下記のとおり解消済み) |
| **B** | `.ofd` に対応キーが無い / ソルバー本体が無い → **カーネル側の作業** | 33 |
| **C** | GUI 層だけで実装できる | 49 |

### A (誤記) について

棚卸しの過程で見つかった A は 2 件で、いずれも `CircuitSolversTab` だった
(「抽出エンジン (PEEC/FEM) の起動は未実装」「ポート定義は計算に渡されません」
と書いてあったが、`runExtraction()` が `peec` / `ofe` を実際に起動していた)。
2026-08-09 の「スミスチャート / S11 表示」の変更で修正済み。

**逆向きの誤記も同じ害がある。** 絶対規則 5 は「未完成を動作済みと見せない」
ことだが、動くものを「未実装」と書けば利用者はその機能に到達できない。
棚卸しは両方向で行うこと。

## 表示の作法 (2026-08-09 に変更)

以前の注記は主語が無く「▸ この設定は現在計算へ反映されません (未実装)」
とだけ出ていた。**節の中に反映される入力と反映されない入力が混在している
場合、利用者は節ごと死んでいると受け取る** (実際にそう報告された)。

現在は主語を必須にしている:

```cpp
tabhelp::unwiredNote(s, I18n::tr("psol_uw_rcwa"), I18n::tr("psol_uw_rcwa_ok"));
// → 「▸ Truncation 法・入射角 (θ, φ, ψ)・TE/TM・出力のチェック —
//     現在計算へ反映されません (未実装)。
//     反映されるもの: 周期・次数・層分割・波長範囲 (.ofdx へ保存されます)」
```

**新しく `unwiredNote()` を足すときは必ず主語のある版を使うこと。**
引数なしの版は既存互換のために残してあるだけで、新規利用は禁止。

## ポスト処理の「どこに出るか」(2026-08-10 更新)

ポスト(1) / ポスト(2) のチェックの出力先は 3 つある。**ev2d / ev3d が無くても
中身が見られる**のは下 2 つ。

| 出力先 | 実体 | 見る場所 |
|---|---|---|
| 作図 | `ev.ev2` / `ev.ev3` / HTML | 中央「カーネル作図」タブ、外部 ev2d/ev3d |
| 2 次元の場マップ | `far2d.log` / `near2d.log` | 中央の 2D 断面 (`CenterPane::loadPostMaps`) |
| 番号付きテキスト表 | `feed.log` / `point.log` / `far0d.log` / `near1d.log` | 結果プロットの「ポスト表」モード |

結果プロットの周波数特性・スミスチャート・放射パターンは `<kernel>.log` と
`far1d.log` から作るので、**ポストのチェックとは無関係**に出る。

## 全件表

### AcousticSourceTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 正規化・遅延・位相のチェックの、ソルバ入力への反映 (.ofd に対応キーがありません) | 基準 SPL と「同時駆動の見積り」— 有効な音源と受音点から距離・到達時刻・補正遅延・受音レベルを出し、無相関/同相の合計とクリップ余裕を出します (3 つのチェックがここに効きます) | **C**/**B** | 2026-08-11 に配線 (core/LevelSum)。ソルバへ渡すにはカーネル側に対応キーが要る |
| このページの設定 (.ofd / .ofdx に対応キーが無くソルバへ渡せません) | 合成された指向性 (鉛直面): ビームパターン図、最大方向、主ローブの 3 dB 幅、−6 dB カバー範囲、主ローブ外の最大、グレーティングローブ出現周波数と超過警告、サブアレイの前後比 | **C** | 2026-08-11 に配線 (acoustics/core/ArrayDirectivity)。ソルバへ渡すにはカーネル側にアレイ入力が要る |
| 出力チャネルのバイノーラル / Ambisonics / 5.1ch / 22.2ch と HRTF データベース (どれも同梱していないため選択不可にしてあります) | 入力 WAV (可聴化のドライ音源そのもの) と受音点リスト (プロジェクトの受音点)。畳み込み方式は FFT 重畳加算で固定 | **C**/**B** | 2026-08-11 に配線。バイノーラルには HRTF データベース、マルチチャネルにはデコーダが要る |

### AcousticTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 鏡像法の反射次数・可視判定とハイブリッド切替周波数 | — | **B** | 鏡像法エンジンが無い (音響コアへ追加が要る) |
| 可聴化の音源選択と出力形式 (モノ / バイノーラル等) | — | **C** | 可聴化タブに実装がある — 導線を繋ぐだけ |

### AnalysisGroupsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| スクリプト言語の選択のうち LSF (Lumerical 本体が要ります) | 作成したグループの一覧と、行に割り当てた Python スクリプトの実行 (作業ディレクトリはプロジェクトのフォルダ、引数に .ofd のパスとグループ名を渡し、出力をログに出します) | **C**/**B** | 2026-08-11 に実行を配線 (QProcess + .ofdx の script キー)。LSF は Lumerical が無いと動かない |

### AntennaCharTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 指向性・放射効率・アンテナ効率・偏波成分/軸比/XPD・アレイ特性のチェック | 放射パターン・主ビーム方向と 3 dB 幅・SLL・前後比・ゲイン (「パターン指標」表と CSV) | **C**/**B** | 2026-08-11 に切断面から出せる 5 項目を配線済み。指向性・効率は全球積分 (far2d) が要り、偏波成分は far1d.log に無い |

### CabinAcousticsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 騒音源のチェックの、伝達経路 (車室への入り方) の扱い | 「騒音源の寄与」表 — チェックした源のレベルをエネルギー加算し、合計・寄与率・その源を消したときの低減量を出します | **C**/**B** | 2026-08-11 に寄与分析を配線 (core/LevelSum)。伝達経路は構造音響連成が要る |
| CAD ファイルの指定と吸音内装のチェック | 下の音響モード計算 (寸法入力から算出しています) | **C** | CAD 取込はジオメトリタブの STL 経路を共用できる |

### ChannelTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 環境モデルの選択とそのパラメータ | — | **C** | em/RadioPropagation に伝搬モデルがある |
| 基地局/AP の台数と受信点の種別 | MIMO 4×4・ビームフォーミングのチェック (チャネル特性表の「アレイ利得」「空間多重の容量上限」) | **C** | 2026-08-10 に配線済み。残りは複数 AP の配置とカバレッジ格子 |

### CircuitSolversTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| メッシュ方式 (自動/手動)・tanδ・周波数点数・適応メッシュ・掃引方式 | ソルバ選択 (解析種別)、PEEC のメッシュ分割/Cp/表皮効果/遅延/周波数範囲と分割数、FEM の RLGC と周波数範囲、および「抽出実行」そのもの | **C** | 2026-08-11 に配線済み。残りは .ofe / .peec に対応キーが無いか意味を持たないもの |
| SPICE 共シミュレーションの設定 (エンジン選択・解析種別・寄生挿入) と共シミュレーション実行 | 結果ページの「SPICE サブサーキット書出」— 集中定数モデル (直列/並列 + R/L/C) を 2 端子の .subckt として書き出します | **C**/**B** | 2026-08-11 に書出を実装 (io/SpiceNetlist の writer)。共シミュレーション実行は外部 SPICE の起動と結果の取り込みが要る |

### EmcTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 試験配置 (サイト種別・アンテナ高・EUT 回転・グランドプレーン) | 測定距離 (限度値の距離換算と上の予測に使われます) | **C** | 試験配置は予測式のパラメータにできる |
| 放射源のチェックとクロック周波数 | — | **B** | 放射源の等価モデルはカーネル入力が要る |
| 伝導エミッションの設定 (LISN / プローブ・検波器) | — | **B** | 伝導エミッションは回路解 (PEEC) 側 |
| 電界分布の可視化と誘導電圧のチェック | 下の「試験レベルから決まる量」(電力密度・ESD 電流) | **B** | 筐体内部電界は FDTD 実行結果が要る |

### FamilySolverTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 他ソルバーとの連携チェック | — | **B** | 連携先のソルバー本体が無い |
| FDTD ページの設定 (互換モード・実行バイナリ・出力) | — | **B** | ソルバー本体が無い |
| RTM ページの設定 (シーン・観測条件) | — | **B** | ソルバー本体が無い |
| THFD ページの設定 (周波数・準静近似・ソルバ・収束条件) | — | **B** | ソルバー本体が無い |
| MoM ページの設定 (導体・基底関数・セグメント長・周波数) | — | **B** | ソルバー本体が無い |
| STF ページの設定 (電極配置ほか) | — | **B** | ソルバー本体が無い |
| トモグラフィページの設定 (アンテナ数・再構成法・領域) | — | **B** | ソルバー本体が無い |

### GeneralTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 整合損失・偏波・反復スキップの 3 チェック (.ofd に対応キーがありません) | — | **B** | .ofd に対応キーが無い (カーネル側で追加が要る) |

### GeometryTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| テセレーション設定 (偏差・角度・品質・並列 / 曲率適応) | — | **B** | STEP/IGES のテセレーションは CAD カーネル依存 |
| 方式のうち「色で」「手動」(取込が面の色を保持しない / 手動は部品表そのもの — どちらも理由を添えて無効化) | 方式「単一材料」「名前で」と既定材料。名前でを選ぶと、部品 (OBJ の g / o / usemtl) ごとに材料番号を割り当ててボクセル化します | **C**/**B** | 2026-08-11 に配線 (MeshImporter がグループを保持するようにし、部品ごとにボクセル化)。色で分けるには取込側が面色を持つ必要がある |
| 細分化手法のうちサブグリッド法と AMR (どちらもカーネル側の対応が要る — 理由を添えて無効化) | 「非均一メッシュ」方式と細分化領域の表、および「自動細分化」ボタン — 領域の端を節点にして内側の分割数を比率倍し、実際の xmesh/ymesh/zmesh を書き換えます | **C**/**B** | 2026-08-11 に配線 (core/MeshRefine)。対象チェック (エッジ/湾曲面/薄膜/高εr) の自動検出はジオメトリ解析が要る |

### IlluminationTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 面の反射モデルの選択 (レイトレーサ本体が未実装のため) | 光学系の構成そのもの (プロジェクトに保存されます) | **C** | レイトレーサは GUI 層で実装できる |

### LayoutGDSTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| PDK の選択とグリッド (実データを持たないため表示のみ) | チップサイズ (変換した形状が収まるかの判定に使います) | **C** | PDK 別の断面データは公開値を持っていない (利用者が z 範囲を入力する形にした) |
| FDTD-IC 連携の 2 つのチェック | 選択レイヤ → 形状ユニットへの変換 (「選択レイヤを形状へ変換」ボタン。多角形を軸平行矩形へ分解し、レイヤーの z 範囲で押し出して直方体にします) | **C** | 2026-08-11 に変換を実装 (io/GdsGeometry)。チェック自体は S パラメータライブラリ側が無い |

### LensEditorTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 解析プロット (Spot / Ray Fan / MTF / 波面 ほか 8 種) と最適化ボタン | 近軸諸元 (EFFL/BFL/FFL/主点/F 値/像高/デフォーカス) と 3 次収差 (ザイデル和: 球面収差・コマ・非点・像面湾曲・歪曲、面ごとの寄与とペッツバール半径)。Merit の値列 EFFL/PIMH/ISFN/SPHA/COMA/ASTI/DIST | **C** | 2026-08-11 に 3 次収差を配線 (optics/SeidelAberration)。残りは実光線追跡 (5 次以上・スポット・MTF) と最適化 |
| 波長サンプルの追加 (プレビューは d 線のみ) | — | **C** | 分散データは GlassCatalog にある — 色収差は追加できる |

### MaterialExplorerTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 非線形・利得・磁性のチェック | — | **B** | 非線形・利得はカーネルの入力キーが要る (磁性は material の amur/msgm で一部可) |

### MonitorsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 同期とレコーダ (位相 / 振幅 / DFT) の設定 | サンプリング周波数 (音響設定へ反映されます) と apodization (結果プロットの「ポスト表 → スペクトル」で、時間波形の端のテーパとして使われます) | **C** | 2026-08-11 に apodization を配線 (core/WaveformSpectrum)。同期とレコーダは .ofd に対応キーが無い |

### MultiphysicsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| このタブの設定すべて (.ofd に対応キーが無く、カーネルへ渡せません) | — | **B** | .ofd に対応キーが無く、カーネルへ渡せない |
| 連成スキームの選択と収束条件 (許容誤差・最大反復) | — | **B** | 連成の反復はカーネル側 |
| 熱解析の設定 | — | **B** | 熱解析キーはカーネル側 (ofd は診断行のみ出す) |
| 生体組織モデルと灌流の設定 | — | **B** | 組織モデルの取込はカーネル側 |
| 振動音響の励振・モーダル解析の設定 | — | **B** | 振動音響ソルバーが無い |
| 海洋環境データの選択 (水温・塩分・流れ) | — | **B** | 海洋データ同化は BELLHOP 側 |

### OpticalTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 解法 (波動 / 幾何) の選択 | — | **C** | 解法の出し分けは GUI の分岐 |
| レイトレースの設定 (レイトレーサ本体が未実装のため) | — | **C** | レイトレーサは GUI 層で実装できる |
| この節の面データ (固定の設計例) と、スポット径・MTF・光線収差図のチェック | 収差解析 (Seidel) — 3 次収差の実計算はレンズエディタタブにあり、この節から誘導する | **C**/**B** | 2026-08-11 に Seidel を実装 (optics/SeidelAberration)。スポット径・MTF・光線収差図は実光線追跡が要る |
| 波動 / 幾何のハイブリッド連携の設定 | — | **C** | 波動側の結果は既に読める |
| この節の設定 | — | **C** | 表示の取捨のみ |

### OptimizeTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 最適化手法の選択 | — | **C** | kernel/SweepRunner が掃引の器を持っている |
| 設計変数の表 (ドメイン別の既定例です) | — | **C** | 同上 |
| 制約条件の設定 | — | **C** | 同上 |
| トポロジー最適化の解像度とフィルタ半径 | — | **C** | 密度場 → 材料分布は GUI 側で作れる |
| 実行先と Pareto 出力の設定 | — | **C** | 同上 |

### PerFaceBCTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 面別の境界条件コンボと対称 / 周期のチェック | — | **B** | .ofd の abc は領域全体に 1 つ (面別指定のキーが無い) |
| PML の詳細チェック | — | **B** | 同上 |

### PhotonicsSolversTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| ソルバ間で数値を突き合わせる比較 | 再実行のチェックと「全ソルバ実行」(チェックしたソルバを順に起動し、終了状態と生成ファイルを表に出します) | **C** | 2026-08-11 に実行を配線済み。比較には共通の観測量への換算が要る |
| FDTD ページの入力 (シミュレーション時間・シャットオフ・サブピクセル・共形メッシュ等) | — | **B** | 対応する .ofd キーが無い項目が主 |
| Truncation 法・入射角 (θ, φ, ψ)・TE/TM・出力のチェック | 周期・次数・層分割・波長範囲 (.ofdx へ保存されます) | **B** | orcwa の入力キーに無い (斜入射は .orcwa 側にはある) |
| 伝搬方向・伝搬距離・境界条件・双方向 / ベクトル / 出力のチェック | アルゴリズム・Δz・n_ref・入射モード | **B** | obpm の入力キーに無い |

### SarTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 機器の選択 (既定値の目安としてのみ) | 送信電力・アンテナ利得・距離・周波数・曝露区分 (「曝露源からの入射量」と指針値の引き当て) | **C** | 2026-08-10 に配線済み。SAR 分布の正規化はカーネルが SAR を出さないので不可 |
| BioHeat・不確かさ・ゾーニングのチェック | — | **B** | BioHeat はカーネル側の解析 |

### ScatteringTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| RCS の出力形式 (モノ / バイ・単位・散乱行列) の選択 | — | **C** | ofd_post の出力から算出できる |
| 近傍界→遠方界変換の設定 (抽出面・広角オプション) | — | **B** | 近傍界→遠方界の抽出面はカーネル側 |
| SE・FSS・吸収率・消光のチェック | Mie のチェック (結果表に完全導体球の厳密解を並べて表示) | **C** | 2026-08-10 に Mie を配線済み。SE・吸収率は 2 回実行の差が要る |

### SchematicTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 回路シミュレーションの設定 (解析モードほか) | — | **C** | optics/PhotonicCircuit が S 行列接続を持つ |
| 温度と熱光学シフトの設定 | — | **C** | 熱光学シフトは屈折率の補正のみ |

### SolverRegionTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 解析領域の指定 (Project / .ofd へまだ配線されていません) | — | **B** | 部分領域の指定キーが .ofd に無い |
| メッシュ細分化・サブピクセル・自動上書きの設定 | — | **B** | 局所細分化のキーが .ofd に無い |
| メッシュ精度スライダの Δt への反映 | Δt (CFL 係数)・シミュレーション時間・自動シャットオフ (「計算へ反映する」ON で .ofd の timestep / solver へ) | **C** | 2026-08-10 に配線済み。残りはメッシュ精度スライダ側 |

### SoundproofTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 改善案のチェック | — | **C** | 遮音の改善量は既存の解析式で出せる |

### Tidy3dTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| サブピクセル平均化と DFT 記録の設定 | 自動 PML (エクスポートに反映されます) | **C** | エクスポータ (io/Tidy3dExporter) は GUI 側 |
| 優先度の設定 (スクリプト生成にも反映されません) | — | **C** | 同上 |
| ローカル計算との比較機能 | — | **C** | ローカル結果は既に読める |

### TransmissionLineTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| このタブの設定すべて (タブ全体が設計モックです) | — | **C** | OpenFEM / OpenPEEC で解ける (回路系タブと同じ経路) |
| 特性インピーダンスの算出法と周波数依存 / 複素表示の選択 | — | **C** | 同上 |
| S パラメータのポート数と出力の選択 | — | **C** | 同上 |
| チェック状態 | — | **C** | 同上 |

### UltrasoundTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| トランスデューサの設定 | — | **B** | 超音波ソルバーが無い |
| チェック状態 | — | **B** | 同上 |
| ビームフォーミングの設定 | — | **B** | 同上 |
| 出力のチェック | — | **B** | 同上 |

### UnderwaterTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| ソルバーの選択と BELLHOP / PE のパラメータ | — | **C** | io/BellhopIO が .env を作る — 項目を増やすだけ |
| 海面の設定 | — | **C** | 同上 (BELLHOP の海面境界) |
| 直上の指向性とビーム幅 | 周波数・音源レベル・距離 | **C** | 同上 (ビームパターン) |
| 損失項の選択と距離の下限 | 距離の上限 (「最大距離」と同期します) | **C** | 同上 (減衰項の選択) |

### VerificationTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 比較ソルバの選択 | — | **C** | 比較実行は Runner を呼ぶだけ |
## `markNotImplemented()` (ボタン無効化) の分布

ボタンは無効化してあるので「押せるのに何も起きない」状態ではない。
実装の優先度を測るための分布のみ挙げる (件数の多い順)。

| タブ | 件数 |
|---|---|
| RoomAcousticsTab | 12 |
| GeometryTab | 9 |
| ThinFilmTab | 7 |
| H5ViewerTab | 6 |
| MaterialExplorerTab | 4 |
| SoundproofTab / OptimizeTab / LensEditorTab / DisplayOpticsTab / AnalysisGroupsTab / AcousticSourceTab | 各 3 |
| Post2Tab / EmcTab / CircuitSolversTab / CabinAcousticsTab / AntennaCharTab | 各 2 |
| その他 15 タブ | 各 1 |

`markNotImplemented()` はツールチップが「未実装」の一言だけで、**なぜ**
押せないのかを言わない。主語付き注記と同じ問題を抱えているので、理由を
取れるようにするのが次の改善点。

## 更新のしかた

このファイルは手で保つ。配線したら該当行を消し、`unwiredNote()` の呼び出し
そのものを消すこと (注記だけ残ると今度は逆向きの誤記になる)。
