# 未反映設定 (unwired) の棚卸し

GUI の設定フォームには「計算へ反映されない」ものが残っている。本書はその
全件表で、`src/tabs/TabHelpers.h` の 3 種類の明示 (絶対規則 5) のうち
`unwiredNote()` が付いた **82 箇所** を対象にする。

## 3 種類の明示

| 表示 | 意味 | 件数 |
|---|---|---|
| `unwiredNote()` | 設定フォームがどこにも読まれていない | 82 |
| `markNotImplemented()` | ボタンを無効化 (押せない) | 81 (呼び出し 62 箇所。うち 1 件は HDF5 無効ビルドでのみ有効) |
| `sampleNote()` | 表示中の数値が実行結果でないことの明示 | 8 |

## 区分

| 区分 | 意味 | 件数 |
|---|---|---|
| **A** | 表示が古いだけで、実際には配線済み | 0 (下記のとおり解消済み) |
| **B** | `.ofd` に対応キーが無い / ソルバー本体が無い → **カーネル側の作業** | 33 |
| **C** | GUI 層だけで実装できる | 49 |

### A (誤記) について

棚卸しの過程で見つかった A は 2 件で、いずれも `CircuitSolversTab` だった
(「抽出エンジン (PEEC/FEM) の起動は未実装」「ポート定義は計算に渡されません」
と書いてあったが、`runExtraction()` が `peec` / `ofe` を実際に起動していた)。
2026-08-09 の「スミスチャート / S11 表示」の変更で修正済み。

**逆向きの誤記も同じ害がある。** 絶対規則 5 は「未完成を動作済みと見せない」
ことだが、動くものを「未実装」と書けば利用者はその機能に到達できない。
棚卸しは両方向で行うこと。

## 表示の作法 (2026-08-09 に変更)

以前の注記は主語が無く「▸ この設定は現在計算へ反映されません (未実装)」
とだけ出ていた。**節の中に反映される入力と反映されない入力が混在している
場合、利用者は節ごと死んでいると受け取る** (実際にそう報告された)。

現在は主語を必須にしている:

```cpp
tabhelp::unwiredNote(s, I18n::tr("psol_uw_rcwa"), I18n::tr("psol_uw_rcwa_ok"));
// → 「▸ Truncation 法・入射角 (θ, φ, ψ)・TE/TM・出力のチェック —
//     現在計算へ反映されません (未実装)。
//     反映されるもの: 周期・次数・層分割・波長範囲 (.ofdx へ保存されます)」
```

**新しく `unwiredNote()` を足すときは必ず主語のある版を使うこと。**
引数なしの版は既存互換のために残してあるだけで、新規利用は禁止。

## ポスト処理の「どこに出るか」(2026-08-10 更新)

ポスト(1) / ポスト(2) のチェックの出力先は 3 つある。**ev2d / ev3d が無くても
中身が見られる**のは下 2 つ。

| 出力先 | 実体 | 見る場所 |
|---|---|---|
| 作図 | `ev.ev2` / `ev.ev3` / HTML | 中央「カーネル作図」タブ、外部 ev2d/ev3d |
| 2 次元の場マップ | `far2d.log` / `near2d.log` | 中央の 2D 断面 (`CenterPane::loadPostMaps`) |
| 番号付きテキスト表 | `feed.log` / `point.log` / `far0d.log` / `near1d.log` | 結果プロットの「ポスト表」モード |

結果プロットの周波数特性・スミスチャート・放射パターンは `<kernel>.log` と
`far1d.log` から作るので、**ポストのチェックとは無関係**に出る。

## 全件表

### AcousticSourceTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 正規化・遅延・位相のチェックの、ソルバ入力への反映 (.ofd に対応キーがありません) | 基準 SPL と「同時駆動の見積り」— 有効な音源と受音点から距離・到達時刻・補正遅延・受音レベルを出し、無相関/同相の合計とクリップ余裕を出します (3 つのチェックがここに効きます) | **C**/**B** | 2026-08-11 に配線 (core/LevelSum)。ソルバへ渡すにはカーネル側に対応キーが要る |
| このページの設定 (.ofd / .ofdx に対応キーが無くソルバへ渡せません) | 合成された指向性 (鉛直面): ビームパターン図、最大方向、主ローブの 3 dB 幅、−6 dB カバー範囲、主ローブ外の最大、グレーティングローブ出現周波数と超過警告、サブアレイの前後比 | **C** | 2026-08-11 に配線 (acoustics/core/ArrayDirectivity)。ソルバへ渡すにはカーネル側にアレイ入力が要る |
| 出力チャネルのバイノーラル / Ambisonics / 5.1ch / 22.2ch と HRTF データベース (どれも同梱していないため選択不可にしてあります) | 入力 WAV (可聴化のドライ音源そのもの) と受音点リスト (プロジェクトの受音点)。畳み込み方式は FFT 重畳加算で固定 | **C**/**B** | 2026-08-11 に配線。バイノーラルには HRTF データベース、マルチチャネルにはデコーダが要る |

### AcousticTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 鏡像法の反射次数・可視判定とハイブリッド切替周波数 | — | **B** | 鏡像法エンジンが無い (音響コアへ追加が要る) |
| 可聴化のバイノーラル / Ambisonics 出力と、無響の音声・音楽素材 (どれも同梱していないため選択不可にしてあります) | ソース音源 — 「クリック」は帯域制限クリックをその場で生成して WAV に書き、可聴化タブと共通のドライ音源に設定します。「カスタム WAV…」は手元の素材を同じモデルへ。出力形式は RIR の実チャネル数から決まる形式を表示します | **C**/**B** | 2026-08-11 に配線 (acoustics/core/TestSignal)。バイノーラルには HRTF データベース、Ambisonics にはデコーダが要る |

### AnalysisGroupsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| スクリプト言語の選択のうち LSF (Lumerical 本体が要ります) | 作成したグループの一覧と、行に割り当てた Python スクリプトの実行 (作業ディレクトリはプロジェクトのフォルダ、引数に .ofd のパスとグループ名を渡し、出力をログに出します) | **C**/**B** | 2026-08-11 に実行を配線 (QProcess + .ofdx の script キー)。LSF は Lumerical が無いと動かない |

### AntennaCharTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 放射効率・アンテナ効率・偏波成分/軸比/XPD・アレイ特性のチェック | 放射パターン・主ビーム方向と 3 dB 幅・サイドローブレベル・前後比・ゲイン (パターン指標表と CSV) と、**指向性** — far2d.log があれば全球積分 D = 4πU_max/∮U dΩ から出します (em/Directivity) | **C**/**B** | 2026-08-11 に切断面から出せる 5 項目、2026-08-12 に指向性を配線。放射効率は入力電力と放射電力の比なのでパターンの形からは出ない (指向性はパターンの定数倍に不変なので出せる)。偏波成分は far1d.log に無い |

### CabinAcousticsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 騒音源のチェックの、伝達経路 (車室への入り方) の扱い | 「騒音源の寄与」表 — チェックした源のレベルをエネルギー加算し、合計・寄与率・その源を消したときの低減量を出します | **C**/**B** | 2026-08-11 に寄与分析を配線 (core/LevelSum)。伝達経路は構造音響連成が要る |
| 吸音内装のチェック (内装ごとの吸音率データが要ります) | 3D モデルの取込 (STL / OBJ / PLY を読み、外接直方体を車室寸法に使います) と、下の音響モード計算 | **C**/**B** | 2026-08-11 に取込を配線 (io/MeshImporter を共用)。STEP / IGES は CAD カーネルが要るため読めないことを明示 |

### ChannelTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 間取り / 地形 STL の読み込みと、材料の透過損失 DB・散乱のチェック | 環境の選択 (屋内 / 市街地 / 車内・車車間 / トンネル) が経路損失の経験式を選び、そのパラメータ (屋内の距離損失係数 N・大都市補正) と併せて「経路損失 (環境モデル)」の行・受信電力・SNR・容量に効きます | **C**/**B** | 2026-08-11 に配線 (em/RadioPropagation に ITU-R P.1238 / 奥村-秦 / COST-231 / 対数距離を追加)。STL の読み込みと材料 DB はレイトレーサ本体が要る |
| AP の配置そのもの (間取りや地形に合わせて置くには配置の入力とレイトレーサが要るため、等間隔の円周配置に固定) | 基地局/AP の台数と配置半径 — カバレッジ図が複数局になり、各点で最も強い局に繋いだときの受信電力・SINR (C/(I+N))・カバー率になります。MIMO 4×4・ビームフォーミングのチェックはチャネル特性表の「アレイ利得」「空間多重の容量上限」、受信点の種別は「格子」でカバレッジ図 | **C** | 2026-08-10 に台数以外を配線、2026-08-12 に複数 AP のカバレッジ格子 (em/RadioPropagation の apRing / coverageMapMulti) を配線。任意配置には間取りデータとレイトレーサが要る |

### CircuitSolversTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| メッシュ方式 (自動/手動)・tanδ・周波数点数・適応メッシュ・掃引方式 | ソルバ選択 (解析種別)、PEEC のメッシュ分割/Cp/表皮効果/遅延/周波数範囲と分割数、FEM の RLGC と周波数範囲、および「抽出実行」そのもの | **C** | 2026-08-11 に配線済み。残りは .ofe / .peec に対応キーが無いか意味を持たないもの |
| SPICE 共シミュレーションの設定 (エンジン選択・解析種別・寄生挿入) と共シミュレーション実行 | 結果ページの「SPICE サブサーキット書出」— 集中定数モデル (直列/並列 + R/L/C) を 2 端子の .subckt として書き出します | **C**/**B** | 2026-08-11 に書出を実装 (io/SpiceNetlist の writer)。共シミュレーション実行は外部 SPICE の起動と結果の取り込みが要る |

### EmcTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 「ケーブル配線を含む」のチェック | サイト種別・測定距離・アンテナ高・両偏波の切替・金属床 (PEC) の模擬 — 限度値の距離換算と、予測のグランド反射 (2 波モデル。完全導体面の反射係数は水平 Γ = −1 / 垂直 Γ = +1) に効きます。ターンテーブルは far1d.log の最大利得が方位の最大に当たります | **C**/**B** | 2026-08-11 に偏波・金属床を配線 (em/RadiatedEmission)。ケーブルは等価放射モデルが要る |
| 放射源のチェックとクロック周波数 | — | **B** | 放射源の等価モデルはカーネル入力が要る |
| 伝導エミッションの設定 (LISN / プローブ・検波器) | — | **B** | 伝導エミッションは回路解 (PEEC) 側 |
| 電界分布の可視化と誘導電圧のチェック | 下の「試験レベルから決まる量」(電力密度・ESD 電流) | **B** | 筐体内部電界は FDTD 実行結果が要る |

### FamilySolverTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 他ソルバーとの連携チェック | — | **B** | 連携先のソルバー本体が無い |
| FDTD ページの設定 (互換モード・実行バイナリ・出力) | — | **B** | ソルバー本体が無い |
| RTM ページの設定 (シーン・観測条件) | — | **B** | ソルバー本体が無い |
| THFD ページの設定 (周波数・準静近似・ソルバ・収束条件) | — | **B** | ソルバー本体が無い |
| MoM ページの設定 (導体・基底関数・セグメント長・周波数) | — | **B** | ソルバー本体が無い |
| STF ページの設定 (電極配置ほか) | — | **B** | ソルバー本体が無い |
| トモグラフィページの設定 (アンテナ数・再構成法・領域) | — | **B** | ソルバー本体が無い |

### GeneralTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 整合損失・偏波・反復スキップの 3 チェック (.ofd に対応キーがありません) | — | **B** | .ofd に対応キーが無い (カーネル側で追加が要る) |

### GeometryTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| テセレーション設定 (偏差・角度・品質・並列 / 曲率適応) | — | **B** | STEP/IGES のテセレーションは CAD カーネル依存 |
| 方式のうち「色で」「手動」(取込が面の色を保持しない / 手動は部品表そのもの — どちらも理由を添えて無効化) | 方式「単一材料」「名前で」と既定材料。名前でを選ぶと、部品 (OBJ の g / o / usemtl) ごとに材料番号を割り当ててボクセル化します | **C**/**B** | 2026-08-11 に配線 (MeshImporter がグループを保持するようにし、部品ごとにボクセル化)。色で分けるには取込側が面色を持つ必要がある |
| 細分化手法のうちサブグリッド法と AMR (どちらもカーネル側の対応が要る — 理由を添えて無効化) | 「非均一メッシュ」方式と細分化領域の表、および「自動細分化」ボタン — 領域の端を節点にして内側の分割数を比率倍し、実際の xmesh/ymesh/zmesh を書き換えます | **C**/**B** | 2026-08-11 に配線 (core/MeshRefine)。対象チェック (エッジ/湾曲面/薄膜/高εr) の自動検出はジオメトリ解析が要る |

### IlluminationTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| TIRレンズ・導光板・蛍光体散乱の 3 要素と、面の反射モデルのうち「BSDF実測」(いずれも追跡モデルに入っていない) | 面の反射モデル「鏡面 / 拡散 / ABG」と、リフレクタ (回転放物面)・拡散板・評価面の寸法。非順次モンテカルロ・レイトレース (optics/IlluminationTrace) に入り、系の全光束・光学効率・ビーム角 (FWHM)・照度均斉度・軸上光度・中心照度になります | **C** | 2026-08-11 に非順次レイトレーサを実装。TIR レンズ・導光板は屈折面/抽出パターンの形状定義、蛍光体は散乱+波長変換の体積モデル、BSDF 実測は測定データが要る |
| CIE 色度図の作図 (ボタンを無効化) | 配光曲線 (極座標)・評価面の照度分布の作図と、IES LM-63 書出 (Type C / TILT=NONE)・EULUMDAT (.ldt) 書出。いずれも追跡結果から作ります | **C** | 2026-08-11 に配線 (widgets/PolarPlot, io/PhotometricIO)、2026-08-14 に EULUMDAT を追加。CIE 色度図は**CIE 1931 等色関数の数表とその出所・ライセンスの確認**が要る (既存の解析近似はスペクトル軌跡の末端が大きく外れる — 700 nm で x = 0.568、実測の表では 0.735)。ボタンのツールチップに理由が出る |

### LayoutGDSTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| PDK の選択とグリッド (実データを持たないため表示のみ) | チップサイズ (変換した形状が収まるかの判定に使います) | **C** | PDK 別の断面データは公開値を持っていない (利用者が z 範囲を入力する形にした) |
| FDTD-IC 連携の 2 つのチェック | 選択レイヤ → 形状ユニットへの変換 (「選択レイヤを形状へ変換」ボタン。多角形を軸平行矩形へ分解し、レイヤーの z 範囲で押し出して直方体にします) | **C** | 2026-08-11 に変換を実装 (io/GdsGeometry)。チェック自体は S パラメータライブラリ側が無い |

### LensEditorTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 解析プロットのうち 波面 (Zernike) と、最適化ボタン | 近軸諸元 (EFFL/BFL/FFL/主点/F 値/像高/デフォーカス)、3 次収差 (ザイデル和: 球面収差・コマ・非点・像面湾曲・歪曲、面ごとの寄与とペッツバール半径)、Merit の値列 EFFL/PIMH/ISFN/SPHA/COMA/ASTI/DIST、スポットダイアグラム (波長ごとに色分けした多色スポット)・光線収差図・色収差の焦点移動 (実光線追跡と波長別の近軸追跡: RMS/最大スポット半径・エアリー半径・けられ本数・像面デフォーカスの注記・軸上色収差) | **C** | 2026-08-11 に 3 次収差 (optics/SeidelAberration)・実光線追跡 (optics/RayTrace)・多波長化を配線。**2026-08-12 に MTF を配線** (`optics/Mtf`) — 回折限界 (円形瞳の自己相関の閉形式) と幾何 MTF (像面交点の特性関数、ビン分割なし) の 2 本を並べて描く。収差込みの回折 MTF は瞳での位相が要るので未実装 (その旨を画面に出す)。**同日に包絡エネルギーも配線** (`optics/EncircledEnergy` — 半径を並べ替えて数えるのでビン幅の誤差が入らない。回折のエアリー包絡は J₁ が要り MSVC に無いため未実装)。**同日に歪曲格子も配線** (`optics/DistortionGrid` — 主光線の実追跡と近軸像高 f'·tanθ の比。回転対称なので方位角は変えず半径だけ写す)。**同日に像面湾曲も配線** (`optics/FieldCurvature` — 像空間で光線は直線なので、像面を 2 枚置いた外挿で交点を閉形式で解く。焦点探索は不要)。最適化は**2026-08-12 にソルバ本体 (`optics/DampedLeastSquares` — Levenberg–Marquardt、差分ヤコビアン、Marquardt の対角スケーリング、箱制約) を実装して検証済み**だが、**2026-08-12 に UI も配線** — 曲率半径が有限の面を変数に、目標 f' を入力して実行する。**結果は自動で書き戻さず**「面テーブルへ適用」を別操作にしてある (利用者の設計値を黙って書き換えないため)。目標に届かなかったときは適用ボタンを有効にせず、その旨を出す。**2026-08-13 にスポット RMS を 2 つ目の目標として追加** — 実光線追跡の RMS スポット半径を残差の 2 本目に足す (残差 = [f' の誤差, 重み × スポット RMS])。f' [mm] とスポット半径 [mm] は桁が違うので**重みを画面に出し、黙って混ぜない**。重み 0.1 / 1 / 10 / 100 でヘッドレス実行して、f' の誤差 0.000372 → 0.0326 → 2.38 → 36.7 mm、スポット RMS 1.9121 → 1.5854 → 1.1922 → 0.2166 mm と**両方向に単調**であることを実測 (画面の「重みを変えるとどちらを優先するかが変わる」の裏取り)。同じ形の最小問題 (残差 [x−a, w·x] → 厳密解 x = a/(1+w²)) を selftest に入れて単調性ごと固定した。スポットを目標に足すと f' はぴったりにならないのが正しい (重み付きの折り合い) ため、**「適用」の可否は f' の一致ではなく残差が初期値より改善したかで判定**し、残った f' のずれを画面に書く (この判定にしないと、スポットを有効にした瞬間に永久に適用できなくなる — ヘッドレス描画で発見)。チェックボックスと重みはセッション限りの UI 設定で `.ofdx` へは書かない (シリアライズ出力は 1 バイトも変わらない)。**同日に変数の個別選択も配線** — 面テーブルに「可変」列 (チェックボックス) を足し、印を付けた面だけを動かす。**1 面も印が無ければ従来どおり曲率が有限な面すべて**を動かすので既定の挙動は変わらない。`.ofdx` は `variable` キーを**印の付いた面にだけ**足す追加拡張で、印なしの出力は従来とバイト一致 (selftest がバイト列で判定)。印を付けても曲率を動かせない面 (Infinity / 0 / 無効) は変数から外し、**何面外したかを画面に出す** (黙って落とさない)。全部が動かせない面だった場合は実行せずその旨を出す。動かした面の番号も結果に書く (「6 面」がどの 6 面か分かるように)。ヘッドレスで 4 通り (印なし / 3 面 / 動かせない面だけ / 混在) を実行し、図のチェック位置と本文の面番号が一致することを確認済み |

### MaterialExplorerTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 非線形・利得・磁性のチェック | — | **B** | 非線形・利得はカーネルの入力キーが要る (磁性は material の amur/msgm で一部可) |
| (解消済み) 実測 n,k テーブルの取込 | 実測の n,k を読み込んで、内蔵材と同じようにフィット・診断・物性値への追加ができる | **C** | 2026-08-13 に配線 (`io/NkCsv`)。CSV / TSV / 空白区切り、`,` `;` TAB 空白のいずれの区切りも、CRLF / LF も受ける。**波長の単位を黙って決めない** — ヘッダの単位語 (nm / um / μm / micron / m) があればそれに従い、無ければ値の中央値の桁で決めて「桁から推測した」と画面に書く。「波長単位」で明示すれば推測しない。**k 列が無いときは 0 ではなく「データ無し」** (`NkSample::k < 0`) として扱い、k の曲線を描かない (0 の直線を引くと吸収ゼロを実測したことになる)。壊れた行は数えて飛ばし、有効な点が 2 点未満なら**全体を失敗**にする (途中までの曲線を返さない)。フィットは**測った点をそのまま**使い等間隔に引き直さない (補間値を実測としてフィットしないため)。ヘッドレス描画で 3 件の不具合を発見・修正: 取込データが「実データ無し」扱いでフィットも追加も押せなかった / 材料を選び直してもフィット範囲が前の材料のままで必ず「範囲が重ならない」になった / 状態表示が取込データにも「公刊 Sellmeier 係数を 64 点サンプル」と出ていた (出所も点数も嘘) |

### MonitorsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 同期とレコーダ (位相 / 振幅 / DFT) の設定 | サンプリング周波数 (音響設定へ反映されます) と apodization (結果プロットの「ポスト表 → スペクトル」で、時間波形の端のテーパとして使われます) | **C** | 2026-08-11 に apodization を配線 (core/WaveformSpectrum)。同期とレコーダは .ofd に対応キーが無い |

### MultiphysicsTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| このタブの設定すべて (.ofd に対応キーが無く、カーネルへ渡せません) | — | **B** | .ofd に対応キーが無く、カーネルへ渡せない |
| 連成スキームの選択と収束条件 (許容誤差・最大反復) | — | **B** | 連成の反復はカーネル側 |
| 熱解析の設定 | — | **B** | 熱解析キーはカーネル側 (ofd は診断行のみ出す) |
| 生体組織モデルと灌流の設定 | — | **B** | 組織モデルの取込はカーネル側 |
| 振動音響の励振・モーダル解析の設定 | — | **B** | 振動音響ソルバーが無い |
| 海洋環境データの選択 (水温・塩分・流れ) | — | **B** | 海洋データ同化は BELLHOP 側 |

### OpticalTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| ハイブリッド (波動 + 幾何) の連携そのもの | 解法の選択 — 幾何光学を選ぶと波動ソルバー (FDTD/RCWA/BPM/FMM) の設定を無効化し、理由と順次光線追跡 (レンズエディタタブ) への誘導を出します | **C**/**B** | 2026-08-11 に配線。ハイブリッド連携には波動と幾何を繋ぐ境界条件の実装が要る |
| 偏波追跡・フレネル係数・分散追跡の 3 チェック (追跡モデルが屈折率と波長を持たないため — 理由を添えて無効化) | レイ数・最大反射回数・最小エネルギー・サンプリング (一様 / ジッタ / QMC / 重要度)・反射モデル・拡散次数 — 「この設定で追跡を実行」が非順次モンテカルロ追跡 (optics/IlluminationTrace) をこの設定で回し、光学効率・評価面へ届く割合・中心照度・ビーム角・打ち切り本数を出します。系そのもの (光源・リフレクタ・拡散板・評価面) は照明タブと共有 (core/IlluminationScene) | **C** | 2026-08-12 に配線。**同日に光線図出力も配線** (`TraceOptions::recordPaths` で先頭 N 本の経路を記録し、r–z 断面に畳んで描く。評価面へ届いた / 外へ出た / 吸収された を色で分ける)。**記録は計測であって物理ではないので、記録の有無で結果が 1 ビットも変わらないことを selftest が判定している**。偏波・フレネルには面の屈折率、分散には波長追跡が要る |
| (MTF は 2026-08-12 に配線済み — 下の「反映されるもの」を参照) | 面データ (レンズエディタタブの面テーブルそのもの — .ofdx に保存され、両タブで共有します) と、収差解析 (Seidel)・スポットダイアグラム・光線収差図・色収差 (いずれもレンズエディタタブで実計算し、この節から誘導します) | **C**/**B** | 2026-08-11 に面データを共有化し、Seidel (optics/SeidelAberration) と実光線追跡 (optics/RayTrace) を実装。**2026-08-12 に MTF (`optics/Mtf`) も実装** — 回折限界と幾何の 2 本 |
| 「モード分解」と伝搬モデルの「BPM」「Physical Optics」(モード分解には境界の界分布そのものが要り、残り 2 つは別の伝搬計算が要ります — 理由を添えて無効化) | 「ガウシアンビーム」と伝搬モデルの「Geometric」— 境界のビーム半径・波長・屈折率から、レイリー長・発散半角・任意距離でのビーム半径・ビームパラメータ積と、**直線で追跡してよい距離** (誤差 1 % になる z) を出します (optics/GaussianBeam) | **C** | 2026-08-12 に配線。モード分解には境界の界分布の受け渡しが要る |
| この節の係数 (材料ごとの実測値は材料タブの担当で、ここはモデルの形を見るための代表値です) | 分散モデルの選択 — Drude / Lorentz / Sellmeier の n(λ) を実際に描き、静的誘電率・共鳴での虚部・群屈折率・長波長極限といった決まった値を出します (optics/DispersionModels。Drude は既存の optics/PlasmaDispersion を呼びます — 同じ物理の実装を 2 つ持たないため) | **C** | 2026-08-12 に配線。材料ごとの実測係数は材料タブ (MaterialDispersion) の担当 |

### OptimizeTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 手法のうち「随伴」「ベイズ」「トポロジー」の選択 | 「掃引」「PSO」「GA」— 実行からカーネルを回します。PSO / GA は 1 世代ぶんをまとめて実行し、評価量で採点して次の世代を作ります (core/Optimizer) | **C** | 2026-08-11 に PSO / GA を配線。随伴はカーネルが感度を返さないので不可 (**B** 寄り)、ベイズは代理モデル (GP) が残り。トポロジーは 2026-08-12 に密度場のパラメータ化を配線したが、反復を回す感度が取れない |
| 変数名と単位の列 (自由記入のラベル) | 対象量・初期値・最小 / 最大 / 分割の列 — 掃引の範囲と PSO / GA の設計変数になります (初期値は 1 個体目の出発点) | **C** | 2026-08-11 に「対象量」列を追加し、平面波 θ / φ・メッシュ倍率・材料 εr 差分に結び付けた。形状寸法を動かすには SweepRunner 側の対応が要る |
| 「物体寸法上限」と「吸音材厚さ上限」(形状寸法を動かす経路が無いので拘束をかける先がありません) | 「対称性」と「最小製造ルール」— トポロジー最適化の密度場に効きます。対称性は鏡像との平均への射影 (充填率は変わらない)、製造ルールは閾値化した形の最小連結長を実測して規則との合否を出します。箱制約は従来どおり変数表の最小 / 最大 | **C** | 2026-08-12 に対称性と製造ルールを配線 (core/DensityField の symmetrize / minRunLength)。寸法上限は形状を動かせるようになってから |
| トポロジー最適化の反復そのもの (カーネルが感度 ∂FoM/∂ρ を返さないので随伴法が組めず、画素数ぶんの設計変数を PSO / GA で回すのは現実的でない) | 設計領域 (原点と大きさ)・解像度・フィルタ半径・射影 β・閾値 η — 密度場のパラメータ化 (core/DensityField) に入り、現在の形状をラスタ化した初期密度場・画素数・最小形状寸法 (フィルタ直径)・充填率・非離散度 M_nd と、フィルタ + 射影後の密度場の図になります。「密度場を形状へ変換」で実際の直方体ユニットになります | **C**/**B** | 2026-08-12 に密度場のパラメータ化を配線。反復にはカーネルが感度を返すこと (随伴法) が要る |
| 実行先の選択 (ローカル / HPC / tidy3d — 投入経路が無いので常にローカル) | Pareto フロント出力 — 各点で 2 つ目の評価量も計算し、非劣解集合 (core/ParetoFront) を結果表の「非劣解」列とトレードオフ曲線の図に出します。ハイパーボリュームも添えます | **C**/**B** | 2026-08-12 に Pareto 出力を配線。実行先は HPC / tidy3d の投入経路が要る |

### PerFaceBCTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 面別の境界条件コンボと対称 / 周期のチェック | — | **B** | .ofd の abc は領域全体に 1 つ (面別指定のキーが無い) |
| PML の詳細チェック | — | **B** | 同上 |

### PhotonicsSolversTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| FDTD / BPM の結果をこの比較に載せること (`<kernel>.log` の Ref[dB] は給電点から見た整合、RCWA の R は平面波の電力反射率 — 定義が違うので突き合わせない。理由を画面に出しています) | 再実行のチェックと「全ソルバ実行」(チェックしたソルバを順に起動し、終了状態と生成ファイルを表に出します) に加えて、RCWA / FMM が出した回折効率 (io/RcwaEfficiency) の突き合わせ — エネルギー保存 R+T=1 の検算と、2 本そろえば共通の周波数軸での最大差・RMS・系統差・相関 (core/SeriesCompare) | **C** | 2026-08-12 に配線。FDTD 側を載せるには平面波の電力反射率をカーネルが出すことが要る |
| FDTD ページの入力 (シミュレーション時間・シャットオフ・サブピクセル・共形メッシュ等) | — | **B** | 対応する .ofd キーが無い項目が主 |
| Truncation 法・入射角 (θ, φ, ψ)・TE/TM・出力のチェック | 周期・次数・層分割・波長範囲 (.ofdx へ保存されます) | **B** | orcwa の入力キーに無い (斜入射は .orcwa 側にはある) |
| 伝搬方向・伝搬距離・境界条件・双方向 / ベクトル / 出力のチェック | アルゴリズム・Δz・n_ref・入射モード | **B** | obpm の入力キーに無い |

### SarTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 機器の選択 (既定値の目安としてのみ) | 送信電力・アンテナ利得・距離・周波数・曝露区分 (「曝露源からの入射量」と指針値の引き当て) | **C** | 2026-08-10 に配線済み。SAR 分布の正規化はカーネルが SAR を出さないので不可 |
| BioHeat・不確かさ・ゾーニングのチェック | — | **B** | BioHeat はカーネル側の解析 |

### ScatteringTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 偏波散乱行列のうち入射偏波と直交する側の 2 要素 (1 回の実行では入射させた偏波の列しか出ない) | モノスタティック / バイスタティックの選択・単位 (m² / dBsm / σ/λ²)・偏波散乱行列の入射偏波の列 — いずれも結果表に効きます。バイスタティックは far1d.log から後方 / 最大 / 最大の角度 / 最小を出します | **C** | 2026-08-11 に配線。直交偏波の列には入射偏波を変えたもう 1 回の実行が要る (2 回の実行を束ねる仕組み) |
| 近傍界→遠方界変換の設定 (抽出面・広角オプション) | — | **B** | 近傍界→遠方界の抽出面はカーネル側 |
| SE・FSS・吸収率・消光のチェック | Mie のチェック (結果表に完全導体球の厳密解を並べて表示) | **C** | 2026-08-10 に Mie を配線済み。SE・吸収率は 2 回実行の差が要る |

### SchematicTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| シミュレーションモードのうち「混合 (光電子共存)」(電気側の回路モデルと光電変換の連成が要るため — 選べないように無効化) | 「周波数領域」— 下の「素子応答」がこのモードの計算です。**2026-08-12 に「時間領域」を配線** (`optics/CircuitImpulse`) — 同じ素子の H(λ) から複素包絡線のインパルス応答を出し、リングなら 1 周ごとの遅延パルス列、MZI なら 2 本のアームが見える。主到達・タップ間隔 (解析値 ng·L/c と並記)・1 周あたりの振幅比を数値で出す | **C** | 混合には電気回路モデルとの連成が要る。搬送波 (193 THz) は標本化できないので時間領域は複素包絡線で扱う (その約束と、窓から溢れた尾の割合を画面に出す) |
| 位相雑音の周波数ごとの分布 (RIN(f) の形。白色周波数雑音以外 (1/f) の情報が要る) | 温度と熱光学シフト (素子応答の共振波長に効きます) と、ショット / 熱 / RIN の雑音項 (受光器の雑音収支になります) | **C** | 2026-08-11 に雑音収支を配線 (core/ReceiverNoise)。**熱光学シフトは以前から配線済みで、ラベルの「(未実装)」が逆向きの誤記だった** — 同日に修正。**2026-08-12 に位相雑音を配線** (`optics/PhaseNoise`) — レーザ線幅 Δν と素子の遅延 τ (素子応答と同じ値) から σ² = 2π·Δν·τ (Wiener 過程の厳密解) を出し、可視度 V = e^{−π·Δν·τ}・コヒーレンス時間・直交バイアスでの相対強度ゆらぎを閉形式で出す。V < 0.1 では干渉が消えかけている旨を断る |

### SolverRegionTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 解析領域の指定 (Project / .ofd へまだ配線されていません) | — | **B** | 部分領域の指定キーが .ofd に無い |
| メッシュ細分化・サブピクセル・自動上書きの設定 | — | **B** | 局所細分化のキーが .ofd に無い |
| メッシュ精度スライダからメッシュを作り直すこと (局所細分化のキーが .ofd に無い) | Δt (CFL 係数)・シミュレーション時間・自動シャットオフ (「計算へ反映する」ON で .ofd の timestep / solver へ)。**2026-08-12 に精度スライダが含意する Δx と Courant 限界 Δt も出すようにした** (`core/FdtdVerification` の `targetCellSize` / `courantLimitDt`) — いまのメッシュから出る実際の Δt と並べて表示し、どちらの数字かが分かるようにしてある | **C** | メッシュ自体の作り直しは .ofd に局所細分化のキーが無いのでできない |

### SoundproofTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 経路別 R の予測 (EN 12354-1 の振動低減指数 Kij) | 経路の R と改善案のチェック — 合成 R'w = −10log10(Σ10^(−R/10)) の入力になり、改善後の R'w と改善量、頭打ちになっている経路まで出します | **C** | 2026-08-11 に配線 (core/FlankingTransmission)。経路別 R の予測には要素の R・接合部の形状・Kij が要る |

### Tidy3dTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| — (解消) | 自動 PML (boundary_spec)・サブピクセル平均化 (subpixel。tidy3d の既定が有効なので外したときだけ subpixel=False を書く)・DFT 記録 (td.FieldMonitor ↔ td.FieldTimeMonitor の切替) — すべて生成スクリプトへ渡ります | — | 2026-08-11 に配線 |
| 優先度をジョブ API へ渡すこと (このスクリプトは送信までは行わないため) | 優先度の選択そのもの — プロジェクトに保存し、生成スクリプトへ注記として書き出します | **C** | 送信そのものを GUI から行う経路が要る |
| 「ローカル CPU と並列実行」と「完了時に通知」(このタブはスクリプトを書き出すだけで送信しないので、並列に走らせる相手も完了を知る手段もありません — 理由を添えて無効化) | 「結果差分の自動チェック」— クラウドの結果 CSV (io/SeriesCsv) を読み、ローカルの `<kernel>.log` の給電点掃引と共通の周波数軸・共通の単位で突き合わせ、最大差・RMS・系統差・相関を出します (core/SeriesCompare) | **C** | 2026-08-12 に配線。並列実行と通知には送信そのものを GUI から行う経路が要る |

### TransmissionLineTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 抽出手法の選択 (準 TEM では V/I・電力・静電容量の 3 定義が一致するので無効化) | 断面種別 (マイクロストリップ / ストリップライン / 同軸 / 平行 2 線 / CPW) と寸法・材料・線路長・周波数・基準インピーダンス。Z₀ / ε_eff / β / v_p / α / 群遅延 / S11・S21 / IL / RL を閉形式で計算します (core/TransmissionLine)。周波数依存と複素 Z₀ の表示も反映されます | **C**/**B** | 2026-08-11 に配線。3 定義の差は非 TEM モードでしか出ないので、そこはカーネル (数値解) の領分 |
| ポート数 (3 ポート以上は回路網の定義が要る) | ポート数 2 の S パラメータと Touchstone .s2p 書出 (周波数掃引 201 点) | **C** | 一様線路 1 本は本質的に 2 ポート。多ポートは回路網モデルが要る |
| 不連続部の 3 項目 (ベンド / ステップ / クロストーク) | γ と S パラメータのチェック — 結果表に出す行の取捨として効きます | **C** | 等価回路モデル (等価 C/L・結合線路) が要る |
| アイダイアグラム | ビットレート・PRBS 次数・遷移時間 → 受信波形の重ね描きと開口・開口幅・ジッタ | — | **2026-08-12 に配線** (`core/EyeDiagram`)。線路の S21(f) を掛けた受信波形を PRBS 1 周期の巡回畳み込み (厳密) で求め、1 UI ごとに折り返す。判定時刻はチャネルの遅延に合わせる (合わせないと絵は開いているのに開口が負になる)。標本化がチャネルを解像できていないときは注記を出す |
| 任意断面の数値解 | — | **B** | 標準断面は閉形式で解けるようになった。任意断面は OpenFEM / OpenPEEC の領分 |

### UltrasoundTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| トランスデューサの設定 | — | **B** | 超音波ソルバーが無い |
| チェック状態 | — | **B** | 同上 |
| ビームフォーミングの設定 | — | **B** | 同上 |
| 出力のチェック | — | **B** | 同上 |

### UnderwaterTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| ソルバーの選択と PE のパラメータ (PE ソルバーのカーネルが無いため) | BELLHOP 側の設定はすべて .env になります | **B** | PE (放物型方程式) ソルバーのカーネルが要る |
| 3D 伝搬 (方位方向の広がり) | TL 断面 (.shd) が 3D シーンにも y = 0 の鉛直面として重なります。海面・海底地形・音源も同じ面の上に描かれ、深さは .env と同じ関数 (`BellhopIO::bottomDepth`) から取ります | **B** | 2026-08-12 に**表示**を配線。解そのものは BELLHOP (2D) なので方位方向の広がりは無い。3D 解には bellhopcxx の BELLHOP3D 対応 (方位角の扇・2 次元地形 .ati/.bty・(x,y,z) 受波器グリッド・3D .shd の読み手) が要る |
| — (解消) | 有義波高と鏡面 / Bragg の選択 → 海面の RMS 粗さ σ = Hs/4 が BELLHOP の SIGMA になります | — | 2026-08-11 に配線。既定は鏡面 (σ = 0) で従来の .env とバイト一致 |
| 実測パターンの読み込み (計測した配光を .sbp として渡すこと) | 指向性の選択とビーム幅 → 射出角の扇 (ALPHA1,2)。さらに「指向パターンを .sbp で渡す」を入れると、指向性=ガウス開口 / アレイ=一様励振の直線開口の閉形式を角度ごとの相対レベル [dB] の表にして &lt;ケース名&gt;.sbp へ書き、RunType 3 文字目を '*' にして読ませます (このとき扇は絞りません)。図と数値 (第 1 ヌル・サイドローブ・等価幅) も同じ式から出します | **C** | 2026-08-12 に .sbp 書出しを配線。**書式は bellhopcuda の読み手 (`src/module/sbp.hpp` / `src/trace.hpp` / `src/util/ldio.hpp`) から確認した**。**2026-08-12 に計測ファイルの取り込み口 (CSV → .sbp) も配線** — `io/BeamPatternCsv` が「角度, レベル」の表を読み (区切り・改行・並び順の揺れを吸収し、ピークを 0 dB へ正規化して平行移動量を表示)、表があれば閉形式より優先して `.sbp` へ書く。残るは円形ピストン (Airy) — J₁ が要り MSVC に `std::cyl_bessel_j` が無いため見送り |
| — (解消) | 体積吸収 (Thorp) → SSPOPT の 'T'、距離の下限 → 受波器距離の始点、距離の上限 → 「最大距離」と同期。幾何拡散は BELLHOP の解に内在するので切り替え不可、散乱・海面反射損失は海面の節の σ が決めるので状態表示のみ | — | 2026-08-11 に配線 |

### VerificationTab

| 未反映の内容 | 反映されるもの | 区分 | 実装に要るもの |
|---|---|---|---|
| 比較ソルバをこのタブから起動すること (各ソルバータブの仕事で、ここから二重に走らせない) | 参照データとの突き合わせ — 給電点掃引 (周波数 vs 反射 Ref[dB]) を参照 CSV と共通の x 軸へ線形補間で載せ替え、単位 (線形 / 電力 dB / 振幅 dB) を揃えてから最大差・RMS 差・系統差・相対 L2・相関を出します (core/SeriesCompare) | **C** | 2026-08-12 に突き合わせを配線。参照は CSV で受けるのでソルバーの出力書式に依存しない (実測値・文献値とも同じ手順で比べられる) |
## `markNotImplemented()` (ボタン無効化) の分布

ボタンは無効化してあるので「押せるのに何も起きない」状態ではない。
実装の優先度を測るための分布のみ挙げる (件数の多い順)。

| タブ | 件数 |
|---|---|
| RoomAcousticsTab | 9 (2026-08-13 に材質 DB 取込と 3D モデル取込 ×2 を実装して 12 → 9) |
| GeometryTab | 7 (2026-08-13 にボクセル化プレビューと 3D プレビューを実装して 9 → 7) |
| ThinFilmTab | 5 (2026-08-13 に GA と層間相関を実装して 7 → 5) |
| H5ViewerTab | 2 (2026-08-13 に重ね描き 2 件と書出への焼き込み 2 件を実装して 6 → 2) |
| MaterialExplorerTab | 3 (2026-08-13 に n,k 取込を実装して 4 → 3) |
| OptimizeTab / LensEditorTab / AnalysisGroupsTab / AcousticSourceTab | 各 3 |
| SoundproofTab | 3 (2026-08-14 に帯域スペクトル CSV を実装して 4 → 3。呼び出し箇所はループを分けたため 1 → 3) |
| Post2Tab / EmcTab / CabinAcousticsTab | 各 2 |
| DisplayOpticsTab | 1 (2026-08-14 に 2 つの図を実装して 3 → 1) |
| AntennaCharTab | 1 (2026-08-14 に `.h5` 書出を実装。HDF5 無効ビルドでのみ 2) |
| IlluminationTab | 1 (2026-08-14 に EULUMDAT 書出を実装して 2 → 1) |
| CircuitSolversTab | 2 (2026-08-14 に Touchstone .s1p 書出を実装して 3 → 2) |
| その他 14 タブ | 各 1 |

### 実装した / 実装しないと決めたもの (2026-08-13)

| ボタン | 判断 |
|---|---|
| RoomAcousticsTab 材質 DB 取込 | **実装** (`io/AbsorptionCsv`)。利用者の「名称, α125…α4k」CSV / TSV を読む。区切りは `,` `;` TAB — **空白は区切りにしない** (材質名に空白が入るため)。α > 1 は残響室法 (ISO 354) の試料端部の回折で普通に起こる正当な測定値なので**捨てず**、そう出ている件数だけ報告する。負値・列不足・名称なしの行は捨てて数える。あわせて **NRC 列を α からの計算へ変更** — 内蔵表はコンクリートだけ「平均を小数 2 桁へ丸めた 0.02」で、残り 8 行の ASTM C423 (250/500/1k/2k の平均を 0.05 刻み) と定義が食い違っていた。定義を `nrcFromAlpha()` の 1 箇所に閉じ込め、画面にも定義を書いた (丸めで 0.00 になる行が「値が無い」に見えないため)。EASE の .xhn はボタン名に挙がっていたが**書式が公開されていない**ので取り込まない旨を画面に明示した |
| RoomAcousticsTab 3D モデル取込 (コンサート / オペラの 2 箇所) | **実装**。既存の `io/MeshImporter` (STL / OBJ / PLY の自前パーサ) で読み、`io/MeshDiagnostics` に**体積 (発散定理 V = Σ v0·(v1×v2)/6)** を足して、体積 V と表面積 S を残響計算のページへ入れる。**閉じていないメッシュには体積が無い** (和は数字を返すが体積ではない) ので `watertight()` が偽なら入れない。そのとき**表面積も入れない** — Sabine は RT = 0.161 V/(S·α) と V と S を対で使うので、片方だけ差し替えると元の V と取り込んだ S が混ざった RT60 が静かに出る (ヘッドレス描画で発見)。メッシュのファイルには単位が無いので m / mm / cm を選ばせ、使った単位を結果に書く。単位を取り違えると入力欄の範囲外になるが、**黙って丸めず**「範囲に入らないので入れていない」と出す (丸めると説明の数字と欄の数字が食い違う — これもヘッドレス描画で発見)。面の向きが内向き (符号が負) なら大きさを使い、その旨を出す |
| ThinFilmTab の最適化手法「遺伝的アルゴリズム」 | **実装**。探索は既存の `core/Optimizer` の実数値 GA (トーナメント選択 + SBX 交叉 + 多項式突然変異 + エリート保存) をそのまま使い、`optics/ThinFilmStack` に探索を書き直さない。初期集団の 1 個体目を現在の膜厚にするので、エリート保存と合わせて**結果が初期値より悪くなることが原理的に起こらない**。乱数は種からのみ決まるので同じ設計・同じ種なら同じ結果。**収束判定を持たないので「収束した」とは言わず**、結果も「反復・収束」ではなく「世代・種」で報告する (同じ欄に別の前提の数字を出さないため文言ごと分けた)。実測: 3 層 AR コートで Merit 1.46085 → 0.594056 (単純降下法は同じ初期値から 0.82647 — 大域探索が別の谷に届いている)。needle / tunneling は**層数を変える**手法で、層の挿入は材料選択と一体の設計判断になるため未実装のまま |
| ThinFilmTab の「層間の誤差相関」 | **実装**。等相関モデル `e_j = σ(√ρ·z_c + √(1−ρ)·z_j)` を製造誤差モンテカルロへ足した。**1 層あたりの分散は ρ に依らず σ²** で、変わるのは層どうしの相関係数だけ。両端が既存の 2 つのモデルと一致する (ρ=0 は層ごとに独立 = 従来の既定、ρ=1 は全層が同じ相対誤差 = 純粋なレートドリフト)。**ρ=0 では共通成分の乱数を引かない**ので乱数列も結果も従来とビット一致 (selftest で検証)。歩留まりの効き方は設計依存で、実測では σ=8% のとき独立 79.5% に対し ρ=1 で 89.2% と**上がった** (共通のスケール誤差はスペクトルを平行移動するだけだが、独立な誤差は干渉の釣り合いを崩す) ため、画面の説明でも方向を主張していない。あわせて歩留まりのバッジに**前提 (σ・系統ドリフト・相関) を必ず併記**するようにした — 3 つの条件で値が変わるので、数字だけでは別の前提の値と見分けが付かない |
| GeometryTab のボクセル化プレビュー | **実装** (`io/VoxelSlice`)。`Voxelizer` が返す直方体 (X 方向にマージ済み) から 1 断面の占有マスクを復元し、追加する前に断面を見られるようにした。**プロジェクトへは何も足さない** (何が入るかを先に見るための機能なので、見ただけで形状が増えると目的に反する)。設定は `currentVoxelOptions()` で本番と共用し、プレビューと実際に入る形状が食い違わないようにした。断面の指定は「面番号」ではなく**固定軸** (0=X/1=Y/2=Z) で受ける — 面番号の意味がタブごとに違い (MeshPreview は 0=XY/1=YZ/2=ZX、H5アニメは 0=XY/1=XZ/2=YZ)、どちらの流儀か分からない引数になるため (実際にこれで取り違えて selftest が落ちた)。面内 2 軸は `H5Reader::seriesSliceAxes` と共用。**全断面の占有セル数を足すと `VoxelResult::occupied` にちょうど一致すること**を selftest の判定にしてある (絵と数が同じものを指している証拠)。等方格子・立方体だけでは軸を入れ替えても検出できなかったので、軸ごとに分割数の違う格子と各辺の長さが違う箱を足した (この判定は共有の軸対応をわざと壊すと 6 件落ちることを確認済み) |
| H5ViewerTab の重ね描き 2 件 (物体オーバーレイ / モニター位置) | **実装** (`io/SliceOverlay`)。物体形状の輪郭と観測点 (`point =` の Probe) を、いま見ている断面へ投影して場マップに重ねる。**断面と交わらないものは描かない** — 別の深さにある物体の輪郭が重なって見えたら、その断面に無いものを在るように見せることになる。点は面積を持たないので、断面 1 枚の厚み (節点間隔の半分) を許容差にし、それより離れた点は描かない。表示範囲の外の点は端へ貼り付けず落とす (位置の誤読になる)。座標 (`/metadata/Xn|Yn|Zn`) が読めないファイルではチェックごと無効。面内 2 軸は `H5Reader::seriesSliceAxes` と共用。実測: 箱 x −0.03..0.01 / y −0.05..−0.01 が u 0.30–0.567 / v 0.567–0.833 (v は上から)、観測点 (0.03, 0.045) と (−0.045, −0.06) が u 0.70/v 0.20 と u 0.20/v 0.90 と手計算に一致し、交わらない断面では 0 個になることを画面で確認した |
| H5ViewerTab の書出への焼き込み 2 件 (凡例 / 形状) | **実装**。書き出す PNG・動画のフレームへ色の目盛と形状の輪郭・観測点を焼き込む。**画面と同じものを焼く** — 形状の投影は画面の重ね描きと同じ `io/SliceOverlay`、凡例の色は場マップと同じ経路 (`FieldCanvas::renderImage`) で作る (別の式で塗ると図と凡例で色が食い違って凡例の意味が無くなる)。目盛の値は書き出しに使った**共通スケールそのもの**なので全フレームで同じ意味になる (フレームごとの自動スケールなら凡例は嘘になるところだが、書き出しは元々共通スケール)。形状の焼き込みは断面の座標が読めるときだけ有効 (画面の重ね描きと同じ規則)。実測: 凡例の目盛 0 / 1.73 が書出スケールと一致し、輪郭と観測点が画面と同じ位置に出ることを書き出した PNG で確認 |
| GeometryTab の取込メッシュ 3D プレビュー | **実装** (`io/MeshProjection`)。ボクセル化する前の「読み込んだ形そのもの」を回して見る。投影 (方位角・仰角の正射影) は純関数にして描画から分け、描画側は面を奥から順に塗るだけ (画家のアルゴリズム)。**厳密な隠面消去ではない**ので面が交差する形状では前後が入れ替わりうる旨を画面に明記した。遠近を付けていないので画面上の長さの比が実寸の比になる (寸法を読む用途)。プロジェクトへは何も足さない。selftest は既知の角度での像 (az=0 なら +Y が画面右・+Z が画面上・+X が奥行き など) と、**投影が回転であること** (単位ベクトル 3 本の像が正規直交) を 8 方位 × 5 仰角で判定する。符号を 1 つ反転させると落ちることを確認済み |
| H5ViewerTab の 3D シーンへの重ね (3 面ビュー) | **実装** (`io/SlicePieces`)。2D で 3 面出ているのに 3D へは主断面 1 枚しか流れていなかったので 3 面とも流す。**直交する 2 枚の面は互いに貫通する**ので四角形のままでは画家のアルゴリズムが成立しない (どう並べ替えても貫通した向こう側が手前に出る領域が残る)。各面を他の面の位置で切ると貫通が無くなり、小片の重心の奥行きで前後が厳密に決まる (直交 3 面で 12 片)。切り分けは純関数にして描画から分け、面内 2 軸は `H5Reader::seriesSliceAxes` と共用。**正規化は全断面で共通の最大値**に揃える — カラーバーが 1 本しかないので面ごとに違う尺度で塗ると同じ色が違う強さを意味してしまう。**弱いところほど透過**させる (場がほぼ 0 の一帯を不透明で塗ると奥の断面も形状も隠れ、3D に重ねる意味が消えるため)。表示の重み付けであってデータの間引きではない旨を凡例に書いた。再生コントロールの状況 (再生中/停止中・コマ位置) も 3D の凡例へ出す。検証は 12 片の描画順を実測し、奥→手前であること・独立に計算した e3 との内積に正の係数で比例すること (残差 1.0e-6/レンジ 250)・各面で最も手前の片が予測どおりの角であることを確認。並べ替えを逆にすると 10.8%、透過を切ると 24.3% の画素が変わる |
| H5ViewerTab の MATLAB 連携 | **実装**。開いている `.h5` の実スキーマから `h5read` 読み込みスクリプト (`.m`) を生成する (Python / Jupyter と同じく**ファイルを作るだけ**で外部ツールは起動しない)。**MATLAB の `h5read` は HDF5 の宣言と逆順の次元で返す** ((Nt, Nx+1, Ny+1, Nz+1, 3) は MATLAB では (3, Nz+1, Ny+1, Nx+1, Nt)) ので、ここを取り違えると転置した絵が出る。生成したスクリプトの主張する軸対応を、**3 軸の寸法がすべて異なる** HDF5 (17×23×29) に対して `h5dump` の実寸法と 1 つずつ突き合わせて検証した (等寸法の格子では x と y の取り違えが検出できない)。`sz(2)` を `sz(4)` に壊すと判定が落ちることも確認済み |
| H5ViewerTab の ParaView 連携 (XDMF) | **保留**。XDMF の仕様は公開されているが、**`/timeseries/E` の軸順が XDMF の `3DRectMesh` の規約と逆**になっている。こちらは (t, x, y, z, 成分) で x が最も遅い軸、XDMF は `Dimensions` を遅い順に書き `VXVYVZ` が (X, Y, Z) の順に対応するため z が最も速い軸である必要がある。**XDMF には転置が無い**ので、そのまま書くと x と z が入れ替わった絵が ParaView に出る (軸ラベルは正しいまま中身だけ入れ替わるので気付きにくい)。正しくするにはデータを転置した実体をもう 1 つ書き出すことになり (元が 100 MB 級になりうる)、「参照するだけ」という XDMF の利点が消えて別の機能になる。手元で ParaView を動かして確かめられない以上、推測で書いて「対応した」とは言わない |
| GeometryTab のアセンブリ「材質一括割当」「小部品を自動無視」 | **保留**。どちらも**部品階層があること**が前提だが、階層を持つのは STEP で、STEP の読み込みは外部 CAD カーネルを要するため入れない方針 (依存を増やさない)。いま扱えるのは STL / OBJ / PLY で、これらは単一のポリゴン集合なので「部品ごとに材質を割り当てる」「小さい部品を除く」の対象そのものが無い。アセンブリツリーは取込済みメッシュ 1 個の実測値だけを出している |
| SoundproofTab の `.dxf` 取込 | **実装** (`io/DxfOutline`)。図面から**仕切壁面積 S** を入れる。DXF は仕様が公開されているが全体は非常に大きいので、ENTITIES セクションの **LINE と LWPOLYLINE だけ**を読む。面積は閉じた LWPOLYLINE の靴紐公式。**読まなかった実体は種類ごとに数えて画面に出す** (黙って無視すると「図面全部を読んだ上での面積」に見える)。DXF の座標に単位は無いので `$INSUNITS` があればそれを使い、無い・0 (unitless) なら**利用者に選ばせる** (推測で m とみなすと面積が 10⁶ 倍ずれる)。輪郭が複数あるときはどれが仕切壁かは図面から決まらないので選ばせ、使った単位と輪郭番号を結果に併記する。**自己交差する多角形では面積を出さない** — 靴紐公式は符号が打ち消し合って「小さいが妥当に見える面積」を返すため。円弧 (bulge) を持つ頂点は数えて「円弧を直線で近似した面積」である旨を出す。**取込は自動実行 (`--screenshot`) では行わない** — 単位と輪郭の選択に対話が要り、押す人が居ない場ではモーダルが待ち続けて CI が診断なしに止まるため (`MainWindow::automation()` で分岐)。検証: 4200×2600 mm の壁と 900×1800 mm の扉を含む図面で S = 10.920 m² / 1.620 m² が画面の欄と注記の両方に出ることをヘッドレス描画で確認。**変異検査で判定の穴が 1 つ見つかった** — 原点に角のある軸平行な長方形だけでは靴紐公式の交差項が消えるので、第 2 項の符号を反転させても素通りする。原点から離した長方形と軸に平行でない三角形を足して落ちるようにした |
| H5ViewerTab の「全体時系列 (max/RMS)」 | **実装**。表示中の断面を全フレームぶん読み直して、コマごとに max = |値| の最大と RMS = √(平均(値²)) を出し、タブ内に `MiniPlot` で描く。**ダイアログにしない** — 自動実行でモーダルを開けない規約があるうえ、断面の統計バッジと並べて見たいため。統計バッジ (現在コマの min/max/平均) とは**別の前提の数字**なので、どの断面の何を何枚から求めたかを注記に必ず書く (平均と RMS は別物なので定義も併記)。時刻が読めれば横軸を秒、読めなければコマ番号にする。GUI スレッドで回すので読むコマ数に上限 (400) を設け、**間引いたときはその旨を出す** (黙って間引くと「全コマを見た結果」に見える)。検証: コマごとに値が解析的に決まる HDF5 (3 軸の寸法をすべて変えて 17×23×29、断面 391 セル中 1 セルだけ 2 倍) を作り、9 コマすべての max・RMS・時刻が解析値と 2e-8 (float32 の格納精度) 以内で一致することを確認。変異検査: RMS を平均|v| に変えると 9 コマすべてが落ちる。あわせて **MATLAB ボタンの文言が動作と食い違っていたのを直した** — 「.mat 変換」とあるが生成するのは h5read の読み込みスクリプト (.m) で .mat ファイルではない (MATLAB は .h5 をそのまま読めるので変換は不要)。「MATLAB (h5read) で開く」に改めた |
| H5ViewerTab の「線積分」 | **実装** (`io/SliceLineIntegral`)。断面上の線分 (面内座標 [m] で始点・終点を入力) に沿った ∫f dl を台形則で求め、線分に沿った分布を `MiniPlot` で描く。**ダイアログにしない** (タブ内に入力欄と図を出す)。**ofd の格子は軸ごとに間隔が変わる**ので、等間隔と仮定せず節点座標を二分探索して実位置から小数添字を出す (等間隔だと思うと粗いところで位置がずれる)。**行 0 は面内 第2軸の + 側** (`H5Reader` の規約。`io/SliceOverlay` と同じ向き) なので行方向は逆順になる — ここを取り違えると上下反転した線を積分する。**線分が断面からはみ出したら false** にして外挿しない。失敗時は「どこまでなら引けるか」(各軸の範囲) を画面に出す。積分値の単位は「値 × m」なので注記に明記。検証: 双一次補間は 1 次多項式を厳密に再現するので、f = a·u + b·v + c なら ∫f dl = 長さ × f(中点) が解析解になる。selftest は**非一様・行列の寸法が違う・原点から離れた**格子 (列 7 点・行 5 点、どちらも不等間隔) で斜め/u 方向/v 方向を判定し、向きを逆にしても値が変わらないこと、範囲外・長さ 0・座標数の食い違いを拒否すること、**中点が「実座標の中点」であって「添字の中点」ではない**ことも見る。変異検査: 行の向きを反転すると 6 件、等間隔格子と仮定すると 6 件が落ちる。ヘッドレスでは |E| = 40 + 100x + 200z の HDF5 (Zn が非一様) を作り、XY 面と **v 軸が非一様な XZ 面**の両方で長さ・平均・積分値・最大が解析値と 3e-10 〜 3e-9 (float32 の格納精度) で一致することを確認した |
| RoomAcousticsTab の「notch フィルタ自動提案」 | **実装** (`acoustics/core/RoomModes` へ `notchCandidates` を追加)。室のモード (剛壁・直方体の閉形式。既存の `rectangularModes` を再利用) のうち**シュレーダー周波数 f_s = 2000√(T60/V) 以下**のものを候補として表に出す。上を切るのは、そこから上はモードが密に重なって個別のモードが応答の山として立たなくなる (統計的な領域) ため。提案 Q はモードの半値幅 Δf = 2.2/T60 から Q = f/Δf (Kuttruff, Room Acoustics 5th ed. §3.3)。半値幅より近いモードは 1 つの山にまとめ、まとめた数を「重なり」として出す。**いちばん大事なのは「鳴く周波数の予測ではない」と明記したこと** — どの周波数でリンギングするかはマイクとスピーカの位置・指向性を含むループ応答で決まり、室のモードだけでは決まらない。注記でその区別を書いている (絶対規則 5 と同じ趣旨)。T60 は画面の他の指標と同じ中音域 (500 Hz / 1 kHz の平均) を使う。検証: selftest 16 件追加。**3 辺をすべて違う長さにした室** (立方体や 2 辺が同じ室では軸モードが縮退して軸の取り違えを検出できない) で、最低モードが最長辺の 1 次軸モード c/(2L) であること・次数が (1,0,0) であること・Q と Δf の式・f_s より上を返さないこと・重なりをまとめた数の合計が f_s 以下の全モード数に一致すること (取りこぼさない) を判定する。変異検査: シュレーダー係数 2000→1000 で 1 件、半値幅 2.2→1.0 で 3 件、まとめ処理の無効化で 1 件が落ちる。ヘッドレス描画では 30×20×12 m の既定ホール (f_s = 29.7 Hz) の 11 候補すべてが閉形式と 7 桁一致することを画面で確認した。**表の行数を増やしても高さが伸びず全行が見えない不具合も描画で見つけて直した** (`makeStaticTable` は生成時の行数で最小高さを決めるため) |
| RoomAcousticsTab の「LF マップ」 | **実装**。STI マップ (`StiMapWidget`) と同じ客席面の格子で、各点の LF を既存の `lateralEnergy` (core/RoomAcoustics) で実計算して色分けする。計算を書き直さず既存関数をそのまま格子に回すだけにしてある。**1 次鏡像法のエコーグラムだけを使った幾何推定**で 2 次以上の反射・拡散反射を含まないことを注記に明記 (`lateralEnergy` のヘッダが要求している表示義務)。色は ISO 3382-1 の望ましい範囲 0.10〜0.35 を赤→緑に割り当て、平均と範囲も併記する。押されたときだけ計算する (格子 × 1 次鏡像法なので常時計算はしない)。検証: ヘッドレス描画で、widget が持つ格子の値が独立に呼んだ `lateralEnergy` と**完全一致** (差 0) すること、**音源が y = W/2 の中心線上にあるので LF が左右対称になる**という自明でない不変条件が 1.4e-16 (機械精度) で成り立つことを確認した (y 軸の添字を取り違えるとここが落ちる)。物理的な傾向も一致 — 舞台に近いほど LF が小さく (直接音が強い)、側壁に近いほど大きい。既定ホール 30×20 m では平均 0.11 と ISO の範囲の下端で、幅の広いホールは LF が低いという既知の傾向どおり |
| RoomAcousticsTab の「BQI マップ」 | **保留**。BQI は**室ごとの公表値を引いているだけ**で (`currentHallView`)、客席面の分布を求める模型を持っていない。1 つの数字から分布を描くと持っていない情報を見せることになるので出さない。無効化の理由文をこの内容に差し替えた (汎用の「作図が未実装」ではなく、なぜ描けないのかを書く) |
| H5ViewerTab の「Schroeder 減衰」 | **実装**。表示中の断面のエネルギーを Schroeder 後方積分した減衰曲線を出す。積分そのものは既存の `acoustics/core/SchroederDecay` (Chu のノイズ補正込み) をそのまま使う。渡すのはコマごとの RMS で、`computeSchroederDecay` が内部で 2 乗するので E(n) = Σ_{k≥n} 平均(値²) になる。**既定オプションはオーディオの RIR (数万サンプル) 向け**で、`minTailSamples` が 256 とコマ数より大きいと末尾ノイズ推定が信号全体を掴み、補正で全エネルギーが 0 になって失敗する (実際に踏んだ) — コマ数に合わせて縮めてある。**「点の受音応答ではなく断面全体のエネルギー」であること、音源がインパルス的でないと減衰曲線として読めないことを注記に明記**。**T20 / T30 は出さない** — 動的レンジと当てはめ区間の判定が要り、それは実測 RIR 解析タブが ISO 3382-1 の手順で行っている (絶対規則 6)。代わりに使える動的レンジ [dB] を出す。時刻 (`/timeseries/time`) が読めないファイルでは時間軸が決まらないので描かない。検証: T60 = 0.5 s の指数減衰を持つ HDF5 を作り、ISO 3382-1 の T20 区間 (−5〜−25 dB) の最小二乗傾きが解析解 −120 dB/s と **1e-6 %** で一致することを確認した。変異検査: 振幅ではなくエネルギーを渡すと傾きがちょうど 2 倍 (−240 dB/s) になって落ちる |
| RoomAcousticsTab の「自動エイミング最適化」 | **保留 (理由を具体化)**。`seatMetrics` は距離とゲインだけの**無指向近似**で、指向性データ (GLL) を持たない。したがって**エイミングを変えても計算される数字は一切変わらない** — 最適化しても差が出ないものを「最適化した」と見せるのは、数字が原因を取り違えて示す典型なので出さない。汎用の「計算エンジンが未実装」から、この内容の理由文へ差し替えた。なお表のエイミング欄は設計受音点への方向で、これは既に自動計算されている |
| RoomAcousticsTab の「GLL ライブラリ」 | **保留 (理由を具体化)**。スピーカーの指向性データを同梱していない。無指向近似のまま機種名だけ並べると、選んだ機種で結果が変わるように見えてしまう。理由文をこの内容へ差し替えた |
| ThinFilmTab の「R/T/A スペクトル」 | **実装**。図には既に R と T が出ていたので、**A を足して R+T+A を 1 枚に**する切替ボタンにした。A は特性行列法が返す `FilmResponse::A` (= 1 − R − T、負にしない) をそのまま使う — タブ側で 1−R−T を組み直すと定義が二重になるため。角度掃引の `AnglePoint` には A が無かったので、`filmResponse` の値をそのまま運ぶフィールドを足した (単一の出所を保つ)。A を出すときは T も必ず出す (R+T+A=1 が読めないと意味が無い)。**吸収が 0 になるのは材料の k がすべて 0 のとき (透明多層膜) で、その場合 A は 0 に張り付く**旨を注記に書いた (既定の AR コートがまさにそれで、線が動かない理由が分からないと不具合に見える)。検証: selftest に **R + T + A = 1** の判定を角度掃引の全点で追加し、`AnglePoint.As` が `filmResponse` の A と一致すること、無損失なら A = 0 (丸め残差 1e-12 以内)、**k > 0 の層を入れた非対称な構成では A > 0 でやはり R+T+A=1** であることを見る。ヘッドレスでは既定 AR コートの 121 波長点で `\|R+T+A−1\|` の最大が **6.7e-16** (機械精度) であること、A を出すと系列が 2 → 6 に増えること、Y 軸ラベルが R/T/A に変わることを画面で確認した。角度-波長マップ・層内電界・FDTD 検証の 3 つは別の計算が要るので無効のまま |
| H5ViewerTab の「FFT スペクトログラム」 | **保留 (理由を具体化)**。この経路の断面データは **|E| (成分の二乗和の平方根) で既に整流されている**。整流された量の FFT は場の周波数成分にならない — 実演すると、sin(2πft) を `\|·\|` にした信号のスペクトルは f が消えて 2f・4f・6f… だけが残る (f = 50 Hz で確認: 元信号のピークは 50 Hz、整流後は 100/200/300/400 Hz)。正しいスペクトルには符号付きの時刻歴が要るが、`readOfdSeriesFrame` は `sqrt(Σ成分²)` を返す実装で持っていない。理由文をこの内容へ差し替えた |
| ThinFilmTab の「角度-波長マップ」 | **実装**。波長 × 入射角の 2 次元 R マップ。**上の曲線とまったく同じ `optics::spectrum` を入射角ごとに呼ぶだけ**にして、別の計算を書かない。格子は 41 (角度) × 81 (波長) の固定 (GUI スレッドで回すので上限を設ける)。**色は 0 を青に固定したまま、その範囲の最大値で正規化する** — 反射率の低い設計 (AR コート等) を 0〜100 % 固定で塗ると一面が青になり、上の曲線には出ている起伏が見えなくなる。0 の意味は変えないので「青 = 反射なし」は保たれ、上限は注記に数値で出す。分光特性で使っている入射角に横の白線を引き、その線上が上の曲線と同じ値であることを注記に書いた。検証: マップの 0° 行が、入射角 0° で解いた 1D スペクトルと **完全一致 (差 0.000e+00)** であることをヘッドレスで確認 — 同じ計算を使っている証拠であり、同じ画面に別の前提の数字が混ざらないことの保証になる。**R + T + A = 1** も 41×81 の全点で最大 6.7e-16 (機械精度)。図の傾向も物理どおり (斜入射で R が上がり、設計波長 550 nm 付近が最小)。層内電界と FDTD 検証の 2 つは別の計算が要るので無効のまま |
| GeometryTab の「八分木 (階層適応分解)」「GPU 加速」 | **実装 (八分木の欄を「探索の高速化」へ差し替え) / GPU は不可**。まず現状を実測した — `Voxelizer` はセル 1 個ごとに全三角形を走査する O(セル数 × 三角形数) で、60³ = 216,000 セルでの所要時間は 三角形 256 → 270 ms、1,024 → 1,129 ms、4,096 → 4,483 ms、9,216 → **10,212 ms** と**三角形数に比例**する。実物の STL (1 万〜100 万三角形) では分単位になるので高速化は実用上必要。ただし入れるべきは**空間分割による探索の高速化**であって、UI が謳っていた「階層適応分解 (最大レベル)」= 出力ボクセルの可変サイズ化ではない — 出力は `.ofd` の直方体で、カーネルは自分の Yee 格子で離散化し直すため、適応的な出力サイズはこのパイプラインで意味を持たない。そこで **`VoxelOptions::spatialIndex` を実装**し、UI の欄も「探索の高速化 / 空間索引を使う」へ文言ごと差し替えた(意味の無い「最大レベル」欄は削除)。レイの向きは RAY_DIR ≈ +X 固定なので Y-Z 平面を一様格子に切って三角形を割り振り、レイの傾き分 (D = |RAY_DIR[a]| × bbox の X 幅) だけ三角形側の範囲を − 方向へ広げて登録する。これで始点 1 点の検索が**必ず超集合**になり、交差回数 (整数) は索引の有無で完全一致する。検証: 立方体 / 傾けた箱 / 球 / 穴あきメッシュ × PVF on/off の 8 通りで、占有セル・直方体数・階段体積・PVF 体積が一致し、**直方体は 1 個ずつ 6 座標まで一致**すること (軸ごとに 13/17/19 分割の非対称格子で判定)。速くなったことは環境に依らない**交差判定回数**で判定する (球で 1,391,040 → 7,462 回 = 186 倍)。三角形 16 倍でも索引ありは判定回数がほぼ横ばい (総当たり ×17.1 に対し ×0.9)。実測の壁時計時間は 60³ セル・9,024 三角形で **3,606 ms → 6 ms**、占有セル数は 24,464 で一致。なお `WindingNumber` は全三角形の立体角の総和なので枝刈りできず、この設定は `RayParity` にしか効かない (UI に明記)。GPU 加速は OpenGL / CUDA を足さない依存方針から不可のため「未実装」表示のまま |
| AntennaCharTab の `.nec` / `.ffe` 書出 | **保留**。遠方界の書出先として並んでいるが、NEC の出力書式も FEKO の `.ffe` も**参照できる公開仕様を確認できなかった**。推測で書式を作ると読めないファイルを「書けた」と言うことになるので着手しない |
| DisplayOpticsTab の「アイボックス輝度マップ」「FOV-効率トレードオフ」 | **実装 (文言を出せるものに合わせて差し替え)**。理由は `kPlot` (作図が未実装) だったが、**作図は GUI 層の仕事**で、必要な数値は `optics/DisplayMetrics` の閉形式に既にある。ただしモックの文言が約束していた「輝度」マップはレイトレース、「効率」トレードオフは RCWA が要り、どちらもこの表で「—」にしている量そのもの。そこで**閉形式で出せるもの**へ文言ごと差し替えた — 「アイボックス幅 vs アイレリーフ」と「FOV-アイボックス トレードオフ」。出せない方 (輝度分布・回折効率) は各図の注記に「レイトレース/RCWA が要るのでここには出さない」と明記。計算は Qt 非依存の `DisplayMetrics` に `eyeboxVsEyeRelief` / `fovEyeboxTradeoff` / `minFovPeriod_nm` を足し、**既存の `waveguideFov` / `eyeboxWidth_mm` を呼ぶ**ので図の動作点は評価表の値と必ず一致する。図はタブ内に描く (別ウィンドウにしないので自動実行でも止まらない)。**途中で物理の思い込みが 1 つ壊れた**: 最初「周期を増やせば FOV は単調に広がる」として判定を書いたら落ちた。実際は fov(Λ) = asin(u+c) − asin(u) (u = 1−λ/Λ, c = n·sinθg,max−1) なので **Λ = 2λ/(1 + n·sinθg,max) に極小があり単調ではない** (同じ FOV を与える周期が 2 つある)。閉形式を `minFovPeriod_nm` として実装し、0.01 nm 刻みの数値走査と一致することを判定に入れた (実測: 閉形式 399.5717 nm / 走査 399.5759 nm)。図も「周期の順」ではなく **FOV の順に並べ替えて**結んでいる (そのまま結ぶと折り返す)。検証: 掃引の各点が閉形式と厳密一致、W(ER) が 0 に当たるまで厳密に直線で以後 0 に張り付くこと、ゼロ交差が ER₀ = L/(2·tan(FOV/2)) と一致すること、アイボックスが FOV の減少関数であること (全点対で判定)、帯域が成立しない周期に旗が立つこと。変異検査 5 件 (掃引を自前の直線に / ゼロ交差の 2 を落とす / 旗を立てない / 極小の閉形式から n·sin を落とす / 固定 ER で計算) は全て検出される。ヘッドレス描画で図と表の一致も確認 (ER 18 mm の点が表と同じ 14.90 mm、目標 10 mm との交点が ER ≈ 23.9 mm、ER₀ = 35.8 mm で 0 に張り付く)。副産物として `MiniPlot::setXMargin()` を追加した (既定 0 なので既存の図は 1 ピクセルも変わらない) — **FOV には achievable な下限があり**、周期が極小の近くだと動作点が必ず左端に来て枠に隠れるため |
| AntennaCharTab の `.h5` 書出 | **実装 (HDF5 有効ビルドのみ)**。保留の理由が「任意依存だから既定ビルドで押せないボタンが残る」だけだったので実装した。そもそも UI 側の理由が `kFormat` (書式の仕様が無い) になっていたのは**逆向きの誤記**で、HDF5 は仕様が公開されているうえ中身のスキーマはこちらが決めるもの。実体は `io/H5Writer::writeAntennaPattern` — CSV 書出とまったく同じ読み取り結果(`io/KernelResultReader`) を入れる。数値を作り直さないので**両方に出る列は必ず一致**する。CSV 側も共通の `readExportSource()` を呼ぶよう直した (同じ読みを 2 箇所に書かない)。構成は `/feed/feed<N>/{frequency,rin,xin,ref_db,vswr}` と `/pattern/cut<N>/{angle_deg,e_abs_db}`、単位・面名・基準インピーダンス・給電点番号は属性。`far1d.log` に偏波成分の列がある面だけ `e_theta_db` / `e_phi_db` を足す (無い面に空配列を置くと「測ったが 0」と読めるため)。**中身が無ければ書かずに false** を返す (0 バイトの .h5 を残して「書き出しました」と言わない)。検証: 実際の `far1d.log` / `<ケース名>.log` の本文をパーサに通してから書き、`H5Reader` で読み戻して全列が厳密一致すること、データセットの一覧が想定と完全一致すること、属性 (単位・面名・周波数・z0・feed_index) が面/給電点ごとに正しいことを判定。`h5dump` でも独立に突き合わせた。変異検査 7 件 (rin↔xin / 全面に 1 面目 / units 省略 / 空でも書く / e_theta に E-abs / z0 を 1 番目で固定 / 偏波列を常に書く) は全て検出される。**HDF5 無効ビルドではボタンは無効のまま**だが、理由は「書式の仕様が無い」ではなく「この実行ファイルは HDF5 を無効にしてビルドされています (-DUSE_HDF5=ON で有効)」に直した |

| IlluminationTab の EULUMDAT (.ldt) 書出 | **実装**。保留の理由は「書式の仕様を確認できていない」だったが、EULUMDAT は**公開されている**ので調べて確定させた (項目 1〜26 のヘッダ、ランプ組 6 行 × n、直射比 10 個、C 角 Mc 個、γ 角 Ng 個、光度 (Mc2−Mc1+1)×Ng 個、CRLF 改行、Isym→Mc1/Mc2 の対応表)。実体は `io/PhotometricIO::writeLdt` / `readLdt` で、IES と同じ `PhotometricData` を使う。**要点は単位**: EULUMDAT の光度は絶対カンデラではなく **cd/1000lm** なので、書きでは総ランプ光束 (lamps × lumensPerLamp) で割り、読みでは同じ基準を掛けて戻す。基準が無い絶対測光は配光自身の光束を基準にする (LORL = 100%)。**対称指定は 1 (軸対称) と 0 (対称性なし) しか書かず、読みも 0/1 以外は推測せずエラー**にする — 2/3/4 は鏡映の向きの解釈が要り、間違えると配光が黙って裏返るため。項目 22 の DFF (下向き光束比) は新設した `partialFlux()` で実際に積分して書く。項目 27 の直射比は**利用率法 (LiTG 3.5) を実装していないので 0**、その旨を書出後の案内に明記した。検証: 一様配光の部分光束が解析値 I·2π(cos g0 − cos g1) と一致すること (**わざと 90° をまたぐビンを作り**、境界で切ることを判定)、等方光源の DFF がちょうど 50%、ランバート光源が 100%、往復で角度・絶対カンデラ・寸法・ランプ数が戻ること、2 本 × 2000 lm の器具で基準が 4000 lm になること (「ランプ 1 本」で割る誤りの検出)、全 C 平面が独立に往復すること、CRLF で書かれること、行数が版面どおりであること。断る側も判定: Isym=2 / 途中で切れた / 数値でない / ランプ組 0 / ランプ光束 0 / 換算係数 0 / 全周をおおわない C 平面。**変異検査 9 件** (正規化を落とす / 基準をランプ 1 本に / 境界で切らない / 長さと幅を入替 / 軸対称を Isym=0 で書く / Isym=1 を畳まない / DFF を全域で積分 / 換算係数 0 を通す / CRLF を LF に) は全て検出される。うち 2 件は**最初は検出できず**、判定を足して塞いだ (DFF はランバート光源では全域積分でも 100% になるため等方光源の 50% を追加、面数の不一致は配光を範囲外まで読んでいたので書き側に突き合わせを追加) |
| ChannelTab の 3 ボタン (ヒートマップ / PDP / .h5) | **3 件とも理由の誤りを訂正 (実装はしない)**。3 つとも `kEngine` (計算エンジンが無い) で括られていたが、**このタブの数値は `em/RadioPropagation` による実計算**で、押せない理由は 3 つとも別だった。(a)「カバレッジヒートマップ」は**すでに同じタブに描かれている** — `propagation::coverageMapMulti` が複数 AP・SINR・カバー率まで計算し、上の「送受信構成」節の `FieldHeatmap` に常時出ている。ボタンは要らないので理由をその案内に差し替えた。(b)「電力遅延プロファイル」は 2 波モデルのタップが**直接波と大地反射の 2 本しかなく**、その 2 本を決める K-factor と遅延差 τ は**すでに上の表に数値で出ている** (`twoRayKFactorDb` / `twoRayExcessDelay`)。2 本の棒を描いても表以上の情報は無く、多タップの PDP には散乱モデルが要る。(c)「チャネル係数 (.h5) 書出」は**H 行列を計算していない** (出せるのは経路損失と K-factor まで) — これだけは本当に無いが、理由を具体化した。**「エンジンが無い」で括ると、実際には計算して画面に出ているものまで無いことになる** |
| AcousticSourceTab の「Air absorption 補償」と「レンダリング実行」 | **2 件とも理由の誤りを訂正 (実装はしない)**。(a) 空気吸収は `kModel` (物理モデルが未実装) になっていたが、**この節が描くのは最大を 0 dB とした相対パターン**で、空気吸収は同じ半径の全方向に共通の減衰だから**厳密に打ち消える** — つまり「実装すれば絵が変わる」ものではなく、**この画面の量には効かない**。数値でも確かめた: 全素子に共通の係数を掛けても正規化パターンは **1e-12 以内で不変**、素子ごとに違う重みなら **1 dB 超**変わる (判定が自明でないことも同時に示す。`tests/acoustics/test_array.cpp`)。理由文を「相対パターンには効かない/補償が効くのは距離を決めたときの周波数特性」へ差し替えた。(b)「レンダリング実行」は `kEngine` (計算エンジンが無い) だったが、**畳み込みエンジンは可聴化タブが持っている** (隣の A/B ボタンは実際にそこへ引き渡している)。理由を「この画面からは実行できません (可聴化タブが行う。A/B で入力 WAV を引き渡せる)」へ差し替えた。なお同じ節の「試聴」「A/B 比較」は**無効ではなく実際に配線されている** (出力 WAV を外部プレイヤで再生 / dry ファイルを渡して案内) — 調査の途中で「押せるのに何もしないボタン」だと誤読しかけたが、コードを読んで取り下げた |
| InteropTab の取込/書出ボタン (全 36 行) | **1 行を実装、5 行の理由を個別化**。表の全行が 1 つのループで同じ `kParser` (読み手が無い) に括られていたが、**その中に読み手も書き手も持っている書式が混ざっていた**。とくに電磁ドメインの取込「`.s*p` Touchstone (測定Sパラ / 検証比較)」は `io/Touchstone` が Touchstone 1.x の MA/DB/RI・HZ〜GHZ を読める。そこで取込を実装した — `.sNp` を読み、**検証タブが実測値を読むのと同じ参照系列 CSV** (`freq_Hz, S11_dB, S11_deg, …`) に変換して保存する。変換は `Touchstone::toCsv` として読み手と同じ場所に置いた。**計算されていない要素は列にしない** (カーネル出力の `column1Only` は S_n1 だけ。0 埋めを列にすると「測ったが 0 dB」と読める) うえ、その旨をファイルの `#` 行と書出後の案内の両方に書く。**厳密に 0 の要素があるときは変換しない** — 20·log10(0) = −∞ で、床値を置けば嘘、`-inf` と書けば読み手がその行ごと落とす (どちらも黙って値を変える)。書出側の 5 行は**別のタブ/メニューに実装がある**ので「この画面からは実行できません (どこでできるか)」へ理由を差し替えた: `.s*p` (ファイルメニューの Touchstone 出力)、`.cir/.subckt` (回路系電磁解析タブ)、`.json` (tidy3d タブ)、GDSII (レイアウト GDS タブ、取込・書出とも)。`.ffe/.ffs` だけは**公開仕様を確認できない**という別の理由に分けた。検証: 2 ポート RI ファイルで列名・行数・dB・度が定義どおり (S = 0.5 → −6.0206 dB、純虚数 → −90°)、**既定の 1・2 列目が周波数と S11 の dB として読み戻せる**こと、列指定で他要素も取れること、`column1Only` では S12/S22 が**ファイルのどこにも現れない**こと、厳密 0 では変換せず理由に要素名が入ること、空データでは作らないこと。変異検査 4 件 (未計算の要素も列にする / dB の係数を 10 に / 位相をラジアンのまま / 厳密 0 を素通し) は全て検出 |
| SoundproofTab の出力 4 ボタン | **1 件を実装、残り 3 件は理由を個別化**。4 つとも `kData` (同梱していないデータ) で括られていたが、**押せない理由は 4 つとも別**だった。とくに「R(f) スペクトル (CSV)」は**データが無いどころか画面に描かれている** — `acoustics/core/SoundInsulation` が 1/3 オクターブ帯域の R(f) を計算し、間仕切壁ページと機器囲いページが曲線にしている。そこで CSV 書出を実装した。実体は新設の `io/BandSpectrumCsv` で、**図を描いたときに控えた値をそのまま書く** (書出のために計算し直さないので図と食い違わない)。書式は `#` 行 + 見出し行 + `freq_Hz,R_dB` の 2 列で、**既存の `io/parseSeriesCsv` (検証タブが実測値を読む入口) がそのまま読み戻せる** — 「書けるが自分でも読めない」ファイルにしないため。シナリオによって量が違う (間仕切壁は R、機器囲いは IL) ので**ボタン文言も「R(f) スペクトル」→「帯域スペクトル」へ**直し、スペクトルを持たないシナリオでは「どのシナリオなら出せるか」を案内する。残り 3 件の理由: レポート (PDF) は `kReport` (様式が未定。加えて PDF 出力には Qt PrintSupport が要り依存方針に反する)、可聴化は `kAudio` (音声出力を持たない方針)、ISO/ASTM 書式は `kFormat` (**有償規格の書式そのもので公開仕様を参照できない**)。検証: 行数が版面どおり、見出しに量と単位が入ること、**書いた CSV を `parseSeriesCsv` で読み戻して周波数・値が厳密一致** (差 0.0)、負値と小数点が壊れないこと (ロケール依存の変換を使っていない)、ラベルに改行を入れても行がずれないこと、空・長さ違いでは**書かない**こと。変異検査 5 件 (桁を落とす / 列を入れ替える / 改行を潰さない / 不正でも書く / コメント記号を落とす) は全て検出。副産物として `tabhelp::saveTextFile()` が**書けたパスを返す**ようにした — 従来は void で、**取り消しても「書き出しました」と言えてしまう**形だったため (既存の呼び出しは戻り値を無視するだけで無変更)。なお `markNotImplemented` の**呼び出し箇所**は 60 → 62 に増えるが、これは 4 個をまとめて無効化していたループを 3 個の個別呼び出しに分けたためで、**無効なボタンは 4 → 3 に減っている** |
| CircuitSolversTab の Touchstone 書出 | **実装 (文言を .s3p → .s1p へ)**。理由が `kEngine` (計算エンジンが無い) になっていたが、**元データも書き手も既にあった** — 結果ページは `em/LumpedRlc` の解析式で Z(f) を出しており (曲線も表もそれ)、`io/Touchstone` には `writeS1p` と `zToS` がある。つまり足りなかったのは 20 行の配線だけ。ただしボタンの文言は `.s3p` (3 ポート) で、**集中定数 RLC は 2 端子 = 1 ポート**なので 3 ポートの S 行列は存在しない → 出せるもの (`.s1p`) に文言を合わせた。実装は **|Z| 曲線とまったく同じ掃引・同じ `rlcImpedance()`** を使う (数値を別に組み立てない) ので、書き出した S11 は画面の曲線と必ず一致する。案内には「これは抽出結果ではなく入力した集中定数モデルの S11」「基準 Z0 = 50 Ω」「点数と範囲」「|S11| の最大」を必ず書く。付随して `RlcImpedance` に**符号つきの Re/Im** を足した (S 変換に要る)。その過程で 2 つ直した: (a) 直列の |Z| が **負の R を二乗して素通し**していた (表は「—」なのに |Z| だけ増える食い違い。`hasR` で切るよう統一)、(b) 無損失並列 LC の完全打ち消しで **0 Ω (短絡) を返していた** — 正しくは開放なので `valid = false` にした。検証: hypot(Re, Im) が既存の閉形式 |Z| と一致すること (両トポロジ 242 点、最大 1e-12)、直列共振で S11 = (R−Z0)/(R+Z0) が実数になること、50 Ω 抵抗が S11 = 0 (< 1e-15)、**無損失 1 ポートは |S11| = 1 が厳密に成り立つこと** (81 点で 1e-15 以内)、受動な RLC は掃引全体で |S11| ≤ 1 (かつ最大が 0.99 を超え最小が 0.9 未満 = 判定が自明でないこと)、.s1p 往復。**リアクタンスの符号は仮定せず数値で確かめた** — 直列は f0 より上が誘導性、**並列はその逆**。変異検査 5 件 (負の R を素通し / 並列の虚部の符号 / 直列の虚部を逆に / 開放の番人を外す / 並列の実部を正規化し忘れ) は全て検出。なお **GUI の書出関数そのものは単体テストが無い** (ファイルダイアログを伴うため) — 部品はすべて検証済みだが、配線の最終確認はヘッドレス起動 (クラッシュしないこと) までである |
| OceanEnvironmentTab の「月 (季節)」と「年平均も併記」 | **理由はそのまま (`kData`)、ただし月の選択そのものを無効化し、表示から月の主張を外した**。無効だったのは「年平均も併記」だけだったが、隣の月コンボは選べる状態で、しかも **`computeSsp()` は月をまったく読んでいない** — プロファイルは海域定数 (sst/sss/type) だけで決まる。月は「表層水温 24°C (7月)」というバッジの**ラベルにしか使われていなかった**ので、月を変えると数字は 1 桁も動かないまま月名だけが変わる。ヘッドレスで実測して確認した (1月と7月で表層 24.0°C・105 m 22.6°C・塩分 34.70 psu が完全一致)。月別気候値 (WOA23 等) を同梱していない以上、季節変化は出せないので**実装はせず**、(a) 月コンボを無効化、(b) バッジを「表層水温 %1°C (代表値)」に、(c) 位置指定の説明に「代表プロファイルは海域ごとに 1 本で季節変化を持たない」と明記、の 3 点に直した。副産物として `tabhelp::markNotImplemented()` の引数を `QAbstractButton*` から `QWidget*` へ広げた (無効化とツールチップは QWidget の機能なので、コンボや入力欄にもそのまま使える。既存の呼び出しは無変更) |
| SarTab / ToleranceTab / UltrasoundTab の出力ボタン 各 3 件 | **理由を `kPlot` (作図が未実装) から `kEngine` (計算エンジンが無い) へ訂正**。着手前に元データの有無を確かめたところ、3 タブとも**描く元の数値そのものを持っていなかった** — SAR は分布計算が無く指標表も「未評価」、公差解析はモンテカルロ試行を回していないので標本が無く、超音波は power-law 吸収を含む波動計算が無いので音場が無い。「作図が未実装」と書くと**絵さえ描けば出せる**と読めてしまい、優先度の判断を誤らせる (逆向きの誤記と同じ害)。実装は見送り、理由文だけを実態に合わせた |

**2026-08-12 に改善済み。** `markNotImplemented()` は理由を必須にした
(`markNotImplemented(b, I18n::tr(tabhelp::notimpl::kXxx))`)。ツールチップは
「未実装 — <何が足りないのか>」になり、よく使う理由は 11 種類の共有キーに
まとめてある (書式の仕様 / パーサ / カーネル側の対応 / 同梱データ / 計算
エンジン / 音声入出力 / 外部アプリ / 実行中の制御 / 報告書の様式 / 作図 /
物理モデル)。**理由なしの呼び出しは selftest の `notimpl-reason` が
ソースを走査して検出する**ので、新しく足すときも必ず理由が要る。

## 更新のしかた

このファイルは手で保つ。配線したら該当行を消し、`unwiredNote()` の呼び出し
そのものを消すこと (注記だけ残ると今度は逆向きの誤記になる)。
